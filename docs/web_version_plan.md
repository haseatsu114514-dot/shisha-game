# ブラウザ版 開発方針・ロードマップ

このファイルは「Claudeをメイン開発者にしてゲームを完成させる」ための
作業方針。新しいセッションを始めたら、まずこれを読むこと。

## 開発の原則

1. **データが唯一の正** — シナリオ・キャラ・フレーバー等は `data/` のJSONに置く。
   ブラウザ版 (`web/`) と Godot 版は同じJSONを読む。シナリオ追加はJSONに書く。
2. **ブラウザ版をメインの開発トラックにする** — Claudeはこの環境で
   ブラウザを起動して自動プレイ・スクリーンショット確認まで自走できる。
   Godot はこの環境で実行できないため、Godot 版は「演出強化版」として後追い。
3. **毎回テストを通す** — `web/test/playthrough.mjs`（優勝ルート）と
   `web/test/screenshots.mjs`（敗北→再挑戦）を変更のたびに実行する。
4. **演出（ジュース）を仕様として明示する** — 「動く」と「気持ちいい」は別タスク。
   新機能には必ず「SE・アニメーション・画面遷移演出」の項目を含めて依頼する。

## ユーザー（オーナー）との分業

| 担当 | 内容 |
|---|---|
| ユーザー | プレイして気になった点を箇条書きで送る／画像生成（キャラ・背景・CG）／方針決定 |
| Claude | 実装・演出・データ組み込み・自動テスト・バランス調整・アセット仕様書/プロンプト作成 |

### フィードバックの送り方（効くやつ）

- 「○○がしょぼい」だけでなく「どうなってほしいか」を1行添える
  （例:「ミックス画面、注ぐたびにボウルに色が溜まってほしい」）
- スクリーンショット添付があると最速
- 1メッセージに何件入れてもよい。Claudeが分解して全部対応する

## アセット差し替えパイプライン

1. Claude が `アセット差し替え進捗管理表.csv` を基に生成プロンプト一覧を作る
2. ユーザーが画像生成AIで作成し、所定のパス（`assets/backgrounds/` 等）に
   同名で上書き
3. Claude が `python3 web/build_data.py && python3 web/build_standalone.py` で
   再ビルド（立ち絵は透過余白を自動計測して表示を正規化する）

現状プレースホルダのままの主な背景: `bg_title` `bg_tournament_stage`
`bg_shop` `bg_home` `bg_naru_shop` `bg_adam_shop` `bg_street_day/night`
`bg_tonari_outside`

## 実装済み（第1章）

- 会話エンジン（dialogue JSON互換・タイプライター・立ち絵2スロット＋自動サイズ正規化）
- 日常ループ（7日×昼夜2行動・バイト・練習・キャラ訪問・スポット解禁制・日替わりカード）
- SMOKE CROWN CUP（機材選択→テーマ→12g配合→パッキング→アルミ穴あけ→炭起こし→
  配置→蒸らし→集中→引き→プレゼン→採点、作業台リグのリアルタイム可視化）
- 演出: WebAudio合成SE・BGM・判定スタンプ・煙/泡パーティクル・結果リビール
- 16:9固定ステージ（スマホ横向き対応）・localStorageセーブ・1ファイル配布ビルド

## 公開URL（プレイ・確認用）

- **開発中ブランチの最新（push 直後に反映・動作確認済み）**:
  `https://raw.githack.com/haseatsu114514-dot/shisha-game/<ブランチ名>/web/dist/shisha_ch1.html`
  - 初回アクセス時に raw.githack の確認ページが出る →「Open the page」を1タップ
- **安定版（未開通）**: https://haseatsu114514-dot.github.io/shisha-game/
  - `.github/workflows/deploy-pages.yml` が `web/dist/` を GitHub Pages へ配備
    （main と claude/** ブランチの push がトリガー）
  - ⚠️ **ユーザーの作業待ち**: Settings → Pages → Source を「GitHub Actions」に
    する必要がある（workflowのトークンでは初回有効化が403で失敗する。
    実行ログで確認済み）。有効化後に再push（空コミットでよい）すれば公開される
- jsDelivr / statically.io は HTML を text/plain で返すため使えない（検証済み）
- スマホは横向き推奨（縦だと回転ヒントが出る）。PC/スマホどちらも同じURLでOK

## 2026-06-11 セッションで実装済み（第6便: 新ヒロイン凛＋デート口実）

- **新ヒロイン: 匂坂 凛（27歳・年上枠・ch1から解放）** — 問屋街の卸直営店
  『Dr.fookah』（旧称「機材ショップ」を改名。1階=物販＋試飲席、2階=ショールーム）。
  海外メーカーNIGHTSIDE日本代理店。はじめを「試香モルモット」として雇う。
  ショップ画面の「2階」ボタンから1日1回・行動消費なしで訪問
  （`ch1_rin_first/second/third/repeat`、RIN_SEQUENCE/doRinVisit）。
  3回目で**限定フレーバー「NS アールグレイ（試作）」解放**（spiceカテゴリ初＝
  余韻/高火力テーマの2マッチが初めて可能になる。requires_flag _rin_sample）
- 凛のLIME（モニター報告 lime_rin_report）、confession_rin、ending（修羅場選択肢・
  sit_with_rin・shuraba_rin・epilogue_rin、CGポイント cg_rin_confession/epilogue）
- **つむぎ取材デート** — lime招待（好感度3）→ outing_tsumugi_1（絵の取材・
  「お店の人を困らせたくない」の一線台詞入り）→ 翌朝フォローLIME
- **デート口実の設計**と**浮気リスクシステムの設計**（ch2-3実装予定）を
  brand/story_and_structure.md に明文化。CLAUDE.md ヒロイン5人化
- ⚠️ **凛の立ち絵が未生成**（chr_rin_*.png）。visual_concepts は characters.json に
  記載済み。立ち絵が来たら make_face_icons.py 再実行で顔アイコンも自動生成

## 2026-06-11 セッションで実装済み（第7便: レギュレーション＋凛デザイン）

- **ch1レギュレーション「ミント2g以上」実装** — 大会・前日リハーサルのミックスで
  課題未達は提出不可（`CH1_REGULATION`/`activeRegulation`、パッキーの告知付き）
- **中間順位発表** — R1/R2終了会話の後に暫定順位パネル（`showStandings`）。
  南雲票が入るまでは演出上**最大2位止まり**（最終採点には影響しない）。
  前園の渋い講評→「総合印象点は最終発表で入る」の引きで逆転の気持ちよさを作る
- **章別レギュレーション表を正史化**（story doc/CLAUDE.md）: ch2=ストロベリー
  （スランプに残酷な高難度）、ch3=自由＋ましろ作戦会議3択、ch4=自由＋日本アピール
  ＋スミさん作戦会議3択、ch5=グレープ単体の技術勝負
- **凛のデザイン確定**: 鈍色ブルーのロング低め結び・無色ネイル・銀フープ・
  深夜だけ黒縁眼鏡。**推しはブルーベリー**（repeatイベントに反映）。
  visual_concepts更新済み（立ち絵生成はこのプロンプトで）
- **ch3控室の「モテモテ」いじり分岐**を設計（修羅場/のろけ/なし、story doc）
- **バッドエンド「ホワイトアウト」仕様**（特殊ED扱い・ロード再開で半減・
  レシピ「二股の林檎」獲得。secret_recipes.jsonに実装済み）

## 2026-06-11 セッションで実装済み（第8便: 勝敗条件＋審査員持ち点投入制）

- **ch1の勝敗条件を変更** — craft（一台の出来）が基準（80）未達なら南雲の持ち点が
  動かず**普通に敗北**。基準超えで最終発表時に南雲が持ち点10を一括投入し
  **必ず逆転優勝**（`NAGUMO_CRAFT_BAR`、finishTournament）。リザルト講評にも反映
- **審査員持ち点投入制**をルールの軸として正史化（story doc/CLAUDE.md）:
  各審査員=持ち点10・好きなタイミングで投入・戻せない。前園は早入れ派、
  南雲は最後まで温存する流儀。**敗者復活戦は余り点が原資**（なるのルートの背骨）。
  中間発表パネルに「実況パネル: 前園R1で3点投入／南雲まだ動かず」を表示

## 2026-06-11 セッションで実装済み（第9便: 用語集＋リーフ規範＋ch5グレープ）

- **用語集（GLOSSARY）実装** — 会話画面の「用語」ボタンで `data/glossary.json` を
  表示（シーシャの仕組み／リーフ3種／道具／作り方／大会用語の5グループ）。
  シーシャ未経験者向けの解説を一級市民として扱う
- **リーフ規範** — 主人公はブロンドのみ（flavors.json に `leaf` フィールド、
  stepMix がフィルタ）。ダーク/シガーは用語・他キャラ演出専用。
  ましろは「ブロンドも作れるがシガー/ダーク好み」を正史化
- **ch5の課題をグレープ単体に変更**（DAはch4の優勝の一台で使用済みのため差別化）

## 2026-06-11 セッションで実装済み（第10便: バイト専用課題）

- **バイトの課題はお客さんのリクエスト**（大会レギュレーションとは別系統）。
  日替わり3種: テーマ希望のみ／指名フレーバー「ブルーベリーしっかり」（2g以上）／
  **「ミントは苦手で……」（ミント0g縛り）**＝接客文化ルール「ミント確認」のゲーム化
  （`baitoRequest`、activeRegulation がモード別に出し分け）。
  リクエストに応えた日は心づけ+500円。ミックス画面の表示・ボタン文言も対応

## 2026-06-11 セッションで実装済み（第11便: 最終調整）

- **ch4=和風一本（DA要素なし）／ch5=ダブルアップル単体に戻す**。
  ch3事件の贖罪とアダムへのリスペクトの回収は**ch5のDA単体勝負**へ移動
  （ch4決勝の一台は抹茶・柚子・ほうじ茶系の「和の煙」に書き換え済み）
- **ミント確認の廃止** — 毎回聞くのは不自然。苦手な客が自分から「抜きで」と言う
  形に（バイトの抜き注文リクエストはそのまま）。お姉さんイベントも客側から
  好みを言う形に書き換え。CLAUDE.mdの文化ルール更新
- **チップ文化の廃止** — 日本のシーシャ屋にチップはない。バイトの臨時収入は
  「店からの売上ボーナス／給料上乗せ／まかない」表現に全面変更。
  お姉さんのチップ描写・金銭報酬も削除（CLAUDE.mdに接客・金銭の規範を新設）

## 大会パートのライブ感 強化アイデア（次回実装候補・優先順）

「動く」は揃ったので、次は「会場にいる感じ」。実装コスト低い順:

1. **パッキー実況のリアルタイム割り込み** — ミニゲームの判定に即反応する実況
   テロップ（perfect→「うおーっと！ 完璧な穴あけ！」／miss→「あらら〜♪」）。
   画面上部に流れる実況バー（ticker）。既存の toast/showStamp を流用すれば数行
2. **観客の声ポップ** — 工程の節目に観客の一言が泡のように湧く
   （「いい匂いしてきた…」「4番テーブルの煙やばくない？」）。
   spawnBubbles の文字版。歓声SEは sfx.js に合成追加
3. **ライバル進捗チップ** — 作業中、画面端に「なる: 蒸らし中」「アダム: 提供完了」
   の小さなステータス表示。自分が遅いと焦る（実害なしの演出プレッシャー）。
   R2調整中に「なる、ここで炭を組み替えたッ！」と実況を挟む
4. **審査員リアクション＋持ち点投入演出** — 提供後に審査員ごとの一言カットイン、
   「前園、3点投入！！」をスタンプ演出で。持ち点制（実装済みのルール）が
   画面で動いて見えるようになる
5. **工程間のマイクロ掛け合い** — ミニゲームの合間に1〜2行の短い掛け合い＋
   マイクロ選択肢（なる「お前、手ぇ震えてんぞ」→「うるさい」/「ありがと」で
   根性/好感度に微反映）。テンポを殺さないよう各ラウンド1回まで
6. **観客ボルテージゲージ** — perfect連続でパッキーのテンション段階UP＋
   会場ゲージ上昇（見た目主体、満タンでプレゼンに微ボーナス）
7. **ラウンドカットイン＆BGM段階展開** — 「ROUND 2」の番組風カットイン
   （day-card流用）。R1→R3でBGMレイヤー追加、結果発表前は無音→ドラムロール
8. **劣勢演出** — 中間発表3位以下で画面がうっすら暗転＋ざわめきSE。
   逆転時のカタルシスの下ごしらえ

## 2026-06-12 セッションで実装済み（第14便: ストーリー深化6点＋ミニゲーム大改修＋家シーシャ）

### ストーリー深化（オーナーのピックアップ6点）
1. **スミさんの沈黙**（`ch2_sumi_silence`・DAY10夜）— サラリーマンの「何か言ってあげないんですか」に「あの顔の男に効いた言葉を、俺はひとつも知らねえ」。ch4特訓「同じ顔をさせたくなかった」の前振り。story doc正典化済み
2. **「店の匂い」ch2配置**（`ch2_lingering_smell`・DAY6夜）— 炭落とし事故の焦げた革の匂いが閉店後の店に残る（説明しない）。前提になるなるの初出セリフ「店ってのは、匂いが染みつくんだよ」を `ch1_naru_fourth` に追加（doc正典が実データ未反映だった）
3. **あげはの空席の回収** — `ch4_ageha_dubai.json` 新規（彼女ルート限定）。ch2決勝『ごめん、明日行けない』の裏側（連勝が直感への自信を折った）を一年越しに本人が明かす。story doc ch2/ch4 両方に正典化
4. **つむぎの煙色チャート正史化** — story doc に全章テーブル（基調=淡いオレンジ／ch1藍→優勝の夜の濁り→ch2灰色→ch3回復→浮気「知らない色」→BAD「ホワイトアウト=無色」。濁り=慢心・guilt系／灰=自己喪失系／白=決定的喪失の3文法）
5. **みんとの嘘の鏡像**（`ch3_minto_small_lies`・DA事件後のLIME通話）—「嘘ってさ、つき始めは小さいんだよ」。彼女の告白シーンとch5贖罪の縦糸を接続
6. **guiltの煙可視化** — `state.guilt` 段階でリグの煙が濁る（CSS `.rig.guilt-1〜3`、最終段は「知らない色」の冷たい紫）。ch2.mjs が検証

### ミニゲーム大改修（「ゲージ止めばかりで単調」対応・オーナー指示）
- **詰めグラム可変**: 12g〜ボウル容量（シリコン12/80beat15/素焼き20、equipment.json に `capacity`）。多いほど味の劣化が遅い（craft+、ただし必要熱量 `needHeat` を満たさないと逆効果）＋**葉代**（大会のみ、価格/10円/g。凛のサンプルは無償）
- **蒸らし＝雑念弾幕**（`runSteamDodge`）: 蒸らしタイマー（早回し表示）が切れるまで♥をドラッグ/矢印キーで雑念ワードを回避。被弾が craft に響く。ch2スランプ中は嫉妬ワード＋高速化。練習ドリル「蒸らしの胆力」追加
- **吸い出し＝温度合わせ**（`stepPull` 全面刷新）: 左→右に走って左へ戻るゲージを止めた位置で上げ吸い/キープ/下げ吸い。最低2回・最大5回で適温ゾーンへ。開始温度は炭・熾き・蒸らし・葉量で決まる。テストは `window.__pullDebug` フックで自動攻略
- **プレゼン工程は廃止**（オーナー決定）: STEP_FLOW から削除、提供の佇まい（魅力/10）のみ残存。r3_end実況・glossary・minigame計画書・CLAUDE.md を同期

### 家シーシャ（第2章〜）
- Dr.fookah に「家シーシャ一式」24,800円（`home_rig_set`、ch2から店頭）。所持中は毎晩寝る前に「一服していく？」（`maybeHomeShisha`）
- **バランス**: 葉代600円／伸びるのは技術・センス・洞察の一番低いもの／通常+2、その日「家で休む」を選んでいれば+3（体力に余裕）、**連夜は+1に減衰**（毎晩吸うのが最適にならない＝チート防止。バイトの有無では縛らない）

### バグ修正（探索して発見した分）
- **oneesan（私服みんと）の正体バレ**: ura_* 立ち絵が未支給の間、通常のみんと立ち絵にフォールバックして見た目で正体が割れていた → 専用差分が無い間は立ち絵非表示（engine.js）
- **なる離反シーンの矛盾**: 「予選を勝ち上がった夜」→「準決勝」（ch2_isolation.json。準決勝後に再生されるため）
- **エイリアス連鎖切れ**: `takiguchi→pakki→（行き止まり）` → `takiguchi→packii` 直結＋ `kumicho→ryuji` 追加
- **噂LIMEが死にデータ**: `ch2_started`/`ch3_started` トリガ4通（組長シガーマン噂・博士人体実験噂ほか）がどの章でも配信不能だった → 章推定＋trigger_day対応＋rumor は好感度ゲート免除（読むと洞察+2）。ch2 DAY3/6 に配信、ch2.mjs が検証
- **ch2_taste_recovery 削除**: 「亜鉛で味覚回復」は廃止済み正典と矛盾する未使用データ
- **face値の誤り14箇所**: `surprised`/`fired_up`/`thinking`/`nervous`/`angry` → 正規9種に修正（ch1_events/ch1_main/ch3/ch4/ending）
- **モブ話者の表示名**: shop_clerk/old_man/customer/everyone を SPEAKER_NAMES に追加（ending.json等のバンドル時に生IDが出るのを予防）

## 2026-06-12 セッションで実装済み（第16便: メッセージ性強化5点＋カメオ2点・全採用分）

オーナー採用指示（ch1ch2全部採用／スティーブ採用／王煙楼モニター採用／シェイク・mukai見送り）:
1. **「顔」の縦糸**: ch1決勝・吸い出し前にサラリーマンさんの顔（`ch1_tournament_r2_end`末尾）→
   ch2バイト結果に「お客さんの顔が頭に残らない」差分（finishBaitoOrder）→
   ch2決勝「誰の顔も、浮かばなかった」（`ch2_empty_victory`。朝スライスindex0-3は不変）
2. **つむぎ「煙は人を覚えてる」**: ch1訪問2回目冒頭に見ないまま当てるビート（色は伏せて「匂いと雰囲気」）→
   ch2 DAY13に「誰が作った煙か、分からなかった」
3. **慢心の客可視化**: 上記1のch2バイト差分
4. **スミさん「技は盗め。ただし、誰から盗んだかは忘れるな」**（チュートリアル結果）
5. **ch1優勝の夜の祝杯が「馬鹿みたいにうまい」**（味覚喪失の逆向き予兆、`ch1_tournament_after`）
- **スティーブカメオ**（`ch2_steve_cameo`）: ch2でDr.fookahに初めて入ると一度だけ。正体は伏せる。
  「強い葉ほど、弱い火」＋東京（ch3会場）への誘い水
- **王 煙楼のモニター映像**（`ch2_rivals_first_sight`冒頭）: 龍のようにうねる煙＋テロップのみ。
  誰も足を止めない（世界はまだ風景）
- 正典化: story doc 指針節（顔の縦糸／継承の対句／逆向きの予兆）・色チャート補足・ch2【起】

## 2026-06-12 セッションで実装済み（第15便: カメオ伏線＋吸い出し調整）

- **ch1 DAY4夜: あげはカメオ**（`ch1_ageha_encounter`、未配線だった既存データを発掘して配線＋
  「フレーバーに例えるの職業病だな」の独白を追加）。買い出し帰りに荷物を拾ってくれる謎のギャル、
  残り香=ホワイトグミベア
- **ch2初対面で回収**: `ch2_rivals_first_sight` を書き換え——匂いで「あの時の人」と繋がる／
  宵野葉子の自己紹介／出場者パスを覗いて「ハジメっち」命名（突然のあだ名呼びだった継続性ギャップも解消）
- **ch1バイトに零-REI-カメオ**（`ch1_rei_cameo`、2回目のバイトで一度だけ）。正体を伏せたV系の客。
  芝居がかった講評と、帰り際の素の「ごちそうさまでした」のギャップ＝健太の核を先に体験させる
- **ch1 DAY9: シガーマン都市伝説LIME**（`rumor_cigarman_urban_legend`、なる発・要LIME交換）。
  噂が ch1都市伝説 → ch2組長 → ch3南雲/あげは → ch3本人 と段階接近する設計に
- **噂LIMEのゲート精緻化**: 部外者（affinity管理外の重鎮）からの噂のみ無条件配信。
  知り合い発の噂は通常のLIME条件（交換済み・trigger_day）に従う
- **吸い出しのペナルティ調整（オーナー指示）**: 最低2回・**3回までは無傷**・4回目以降は
  1回ごとに craft -3（「葉が痩せる」）。やめ時は自分で選ぶ。UI に⚠警告、用語集・CLAUDE.mdも更新
- 正典化: story doc（香りの識別子=アゲハはホワイトグミベア、Ch1の引き#7〜9）・CLAUDE.md ID表

## 次回への引き継ぎ（2026-06-14 → Codexへ）★まずここを読む

**この引き継ぎは Codex がリポジトリと一緒に受け取る前提でまとめた、自己完結の作業ガイド。**

### 0. まず読む順番
1. **`CLAUDE.md`**（プロジェクト規約・正史・**ブラウザ版の開発ワークフロー**＝ビルド/テストの正確なコマンド・禁止事項）。
2. **`docs/owner_requests.md`**（オーナー要望の**生きた台帳**＝完了/残/保留が常に最新。新しい要望が来たらまずここへ1行追記してから着手）。
3. このセクション（全体像）。詳細仕様は `docs/master_spec.md` 第2部（大会）・`brand/story_and_structure.md`（章別）を必要分だけ。

### 1. 現状（重要）
- 開発ブランチ **`claude/great-galileo-omzoob`**（PRでmainへ・**直push禁止**）。**全7テスト緑**で push 済み。
- メイン開発トラックは**ブラウザ版 `web/`**。**Godot版は当面保留**（オーナー指定・余裕が出たら移植）。Godoo側は触らない。
- ビルド: `python3 web/build_data.py && python3 web/build_standalone.py`（要 `pip install Pillow`）。`data/` か `web/` を変えたら**必ず両方**。
- テスト: `python3 -m http.server 8123` 起動後 `node web/test/<name>.mjs` を7本（playthrough/ch2/screenshots/reel/kuji/balance/map_hover）。重い3本は**1本ずつ**。playthroughは ~6分。

### 2. このセッションで完了・push 済み（要約）
- **日常ループUX**: 日付整合（`daysUntilTournament()`＋`{daysLeft}`トークンに一本化）・スキップ貫通停止・午後の予定の一拍・スロット/LIME/DAYカードの順序（LIME消化後に回す）。
- **スロット(reel.js)**: 役名＋恩恵の読み上げ・**天井15回**＋抽象的な示唆（数値は出さない）・リプレイの間・パイナップル優遇・**直近3件被らないpick()**・熱い赤/レア金桃の色分け・外れの楽しい/レア台詞。
- **大会演出**: 実況/可視スコア(体感スコア)/ニコ動コメント（**右→左へ全幅cqw**）/結果カット割/リザルト内訳/入場。**#26ライバル実況フィード**（工程ごとに誰が何を完了したか進行表示）。
- **大会の山場**: 結果の順序整合（審査=南雲の二口→「もうだめだ」despair→10カウント→総合印象点満点で逆転）。ライバル基礎値↑で**実力は3〜4位・1位は南雲票のみ**。大会後LIMEの突きつけ役を**なる→スミさん**に。
- **ミニゲーム**: 吸い出しJUST難化（達成感）・**3-2-1カウントダウン**（`miniCountdown()`・炭起こし/集中）・**アルミ穴あけ改修**（周回減点撤廃→穴数で抜け⇄熱トレードオフ・`projectedTemp`に穴数項基準18）。
- **育成**: **#42 章ごとソフトキャップ `statSoftCap()`**（ch1≈42…ch5=100・gainStatとスロット両方に適用）。
- **日常系**: **#41 体力蓄積**（夜回復25・家で休む=フル+overworkリセット・恋人=好感度で回復）／**#44 フレーバー持参制**（Dr.fookahで仕入れ／未仕入れはスミのお情けDA・**強制購入なし**）／**#36 マップ初回チュート**（点滅・非ブロッキング）。
- **#9 tonari統合**: バイト/練習/スミさん/常連席を **tonari 1スポット**に集約（`TONARI_SPOTS`＋`showTonari()`サブメニュー）。**テスト3本の巡回も2段ナビに改修済**（pin→`#tonari-menu .spot-btn`）。
- **ナラティブ**: 主人公を**勤務3ヶ月目**に統一・みんと初登場の戸惑い＋買い物イベント作り直し（初の素）・**アダムのカタコト修正**（LIMEに残ってた）・なるの敗者復活背景の書き換え（廃止と矛盾解消）・みんとrival→supporter転換。
- **docs**: CLAUDE.md にワークフロー明文化・**owner_requests.md（要望台帳）新設**。

### 3. 残タスク（owner_requests.md が一次ソース）
- **#17** ch2日常会話 ／ **#16** ch2マップ（1章エリアへも移動）＝ch2の日常ループ/SPOTS構造に踏み込む。要調査。
- **#53** ステがミニゲームに効く落とし所＝**オーナーが設計検討中（保留）**。勝手に実装しない。決まり次第。
- **#46** 調整吸い（R2で炭替え＋調整吸い・適温ゲージ共通）＝設計確定済み・未着手。
- データランプ（役の記録）のメニュー閲覧（好きな人向け・程度）。
- 素材待ち: お祈りカット/入場立ち絵/大会本番背景/凛の立ち絵（来たら差すだけ）。

### 4. Codex向けの落とし穴・前提（コードに書いてない約束）
- **テストフックを壊さない**: `__pullDebug`/`__heatDebug`／R2の「このままでいく」／審査の `.trial-appeal[data-backed][data-cat]`＋`#trial-doubt[data-need]`／結果カウントの `pointer-events:none`／**tonariサブは `#map-pins .spot-pin`(tonari)→`#tonari-menu .spot-btn` の2段**（テストもこの順）。
- **数値を見せない**: ステは★と抽象語のみ。天井示唆も「近い…！？」等の抽象表現（数値NG）。旧ステ名（技術力/洞察力/メンタル/度胸/味覚/集中力）禁止。
- 状態は単一の `state`（`save()`/localStorage `shisha_ch1_save_v1`）＋大会中は `tt`。別パターンを増やさない。
- 大会の生放送レイヤーは `broadcastActive()`（mode==="tournament"）でガード＝チュートリアル/バイト/ドリルには出さない。
- タイミング系ミニゲームは**必ず `miniCountdown()`** を挟む。
- 「DAYカウントが7のまま」等のオーナー報告は**旧セーブ/キャッシュ残留**の可能性（現コードは `daysUntilTournament()` で正しい・DAY11→あと4日を検証済）。ハードリロード/New Gameを案内。セーブ版数ガードは未実装＝入れてもよい。
- 大会フロー: setup→theme→mix→pack→foil→coal→coalfire→steam→adjust→focus→pull→flavorTrial→finishTournament→showResult→runResultCountdown→（ch1勝ち時のみ judging→count→reveal）→表彰→postClearPhone。

---

## 次回への引き継ぎ（2026-06-13 第26便終了・大会演出オーバーホール＋日常UX）

**開発は `claude/great-galileo-omzoob`（PRでmainへ）。この便は3コミット push 済み・全テスト緑
（playthrough/ch2/screenshots/reel/kuji/balance/map_hover）。**

### この便で完了・push 済み
1. **日常ループUX修正バッチ**（オーナーのプレイ報告・画像の日付矛盾＋4点）:
   - **日付整合（#35ほか）**: 旧7日制の名残で台詞/LIMEの「あとN日」が14日制HUDと矛盾していた
     （画像: DAY5 HUD「あと10日」vs スミさん「あと3日」）。HUD/会話/LIME を `daysUntilTournament()` に一本化し、
     会話JSONは `{daysLeft}` トークンを実数差し込み（engine.js に interpolate フック・LIMEは addLimeBubble）。
     直した台詞: ch1_opening / ch1_day5_sumi_story / DAY7折り返し / lime_minto_day5 / lime_naru_day10。
   - **スキップ貫通**: morningPhone / playLimeEvent で stopAutoSkip()。会話を飛ばしても夜の誘い/朝LIMEは頭から見せる。
   - **午後の予定の唐突さ**: endAction の pendingLimeNight に「夕暮れ／約束の時間だ」の一拍。
   - **スロット×LIME×DAY被り（#34/#23）**: advanceDay で `showMap({skipReel:true})`→朝LIME消化→スロット精算の順へ。
     誘い/デート行動もスロットを回す（#33・closePhone と night invite で REEL.onAction）。
   - **リプレイ再回転が一瞬（reel.js）**: リプレイ連鎖を速回し対象から除外（`_afterReplay` タグ）＋「リプレイ！もう1回転」の間。
2. **大会演出オーバーホール（骨組み・本番大会のみ）**:
   - **#24 可視スコア＋ニコ動コメント**: 「体感スコア」を新設（審査員票と別系統）。判定で `+N P` ポップ＋
     ニコ動風コメントが流れる。`feelPop/nicoComment/nicoBurst/broadcastJudge`、CSS `#tn-comments/#tn-feel`。
   - **#20 工程ブロックMC実況 / #26 ライバル実況**: `mcBlockIntro` を tournamentStep にフック（ticker＋ライバルの動き）。
   - **#14 結果10カウントのカット割**: カウントの周りに出場者の「お祈りカット＋作った一台（色）」。
     `runResultCountdown(rank,premium,onDone,contestants)`＋`contestantShishaColor`。#47 暗転→プチュン / #48 “発表前”文言。
     （カウント数字が消えていた不具合も修正）。
   - **#25 大会後リザルト内訳**: 結果画面に工程A/B/C＋手がかり＋体感スコア。`tournamentBreakdown/buildBreakdownPanel`。
   - **#27 入場演出**: ライバル→主人公をプロレス風に順次紹介＋各カードにスミさんの事前ブリーフィング。
     `entranceIntro`＋`ENTRANCE`、startTournament の opening 後にフック。自動送り（テスト安全）。

### ⚠️ 素材待ち（骨組みで実装済み・画像が来たら差すだけ）
- **#14 お祈りカット / #27 入場の立ち絵**: 現状は顔アイコン＋名前＋色（一台）＋異名／スミ解説で仮表示。
  お祈り/入場の専用立ち絵が来たら `runResultCountdown` の `.rc-pray` と `entranceIntro` の `.ent-cut` に差す。
- bg_tournament_stage 等の本番背景プレースホルダも未差し替え（直接呼びのスクショが白飛びするのはこのため・実機は暗背景でOK）。

### 大会演出で“まだ”のもの
- **#46**（R2にも吸い出しと同じニードル温度ゲームを入れるか）= オーナー要再判断（3択・前回dismiss）。
- ライバルの実況/お祈り/入場は ch1 のみ（ch2+ は RIVALS/ENTRANCE をその章用に増やせば流用可）。

### 大会以外の積み残し（下の旧セクション群に詳細）
- **#53 ステ→ミニゲーム反映**（設計タスク・序盤難→育成で楽）／ **#41 体力蓄積制** ／ **#44 フレーバー持参制** ／
  **#42 ch1ステ上がりすぎ微減** ／ **#2/#3/#31 みんとナラティブ** ／ **#9 tonari統合** ／ **#16/#17 ch2マップ・日常会話** ／
  **#43残 なるの敗者復活背景** ／ **#36 マップ初回チュート**。

### 進め方の推奨 / テスト互換のキー
- contained修正はその場で→fixごとに build2本＋playthrough。ビルド: `python3 web/build_data.py && python3 web/build_standalone.py`（要 Pillow）。
- テスト: `python3 -m http.server 8123` 起動後 `node web/test/<name>.mjs`。playthrough は大会が長く ~6分。
- 大会フロー: setup→theme→mix→pack→foil→coal→coalfire→steam→adjust→focus→pull→flavorTrial→finishTournament→showResult→runResultCountdown→reveal。
  生放送レイヤーは `broadcastActive()`（mode==="tournament"）でガード＝チュートリアル/バイト/ドリルには出ない。

## 次回への引き継ぎ（2026-06-13 第25便終了・コンテキスト引き継ぎ）

**コンテキスト圧迫のため新セッションへ。開発は `claude/great-galileo-omzoob`（origin/main の約30コミット先・全テスト緑）。
オーナーがプレイ通しで挙げた指摘 #2〜#52 を、A1完成後に大量に潰した便。残りは下の各セクションに全部ある。**

### 開始手順
1. shisha-game 起点 → `claude/great-galileo-omzoob` をチェックアウト。`./tools/check_git_safety.sh`／hooks有効化／canonicalRoot設定。
2. `pip install Pillow` → `python3 web/build_data.py && python3 web/build_standalone.py`。
3. `python3 -m http.server 8123` 起動 → `node web/test/<name>.mjs` で6本（playthrough / ch2 / screenshots / reel / kuji / balance / map_hover）。**全緑が基準**。
   - 注: playthrough は大会が長くなったため1本 ~6分。`tail -2` だと進捗が見えないので素のログで。
4. CLAUDE.md（特に「大会ルール=2位以下GAME OVER・敗者復活廃止」「プレゼン廃止→FLAVOR TRIAL」「ステ5種」「数値非表示」）と docs/master_spec.md 第2部。

### 第24-25便で完成・コミット済み（全テスト緑）
- **★A1 大会パート4要素 完成**: HOLE穴あけ / HEAT炭起こし / **FLAVOR TRIAL審査** / **RESULT 10カウント**。
- 会話: 改行禁則(名前割れ/孤立回避)・**改ページ(最大2行)**・ウィンドウ**24px/152px固定**・改行エディタ(`web/tools/linebreak_editor.html`)。
- マップ: ロック地点の視認性・ピン埋もれ(z-index)・誘い日の同店2回。穴あけ狙い表示。
- **スロット MOKUMOKUパッキー**: 行動後に自動回転・脱アプリ(introスキップ)・狙え撤去・バケ=7-7-BAR。reel.js は配線済み。
- 大会制作: 炭は「セット→7割焼けからスタート取り出し」・**適温=テーマ依存**・**R2で温度可視化＋炭で寄せる**・
  炭2個削除/4個で大昇温/フラット・キューブ炭で温度差・炭焼き順ランダム・ジャスト色(赤さ残し)・穴あけ切り上げ(1/2/3周)・調整の整合。
- 章タイトル「一吸目」・敗者復活廃止の文言反映。

### 残り＝大きく4群（★が最重要。詳細・対象ファイルは下の旧セクション群）
1. **大会演出オーバーホール（★一括が綺麗・互いに連動）**: #20 工程ブロック分割＋MC実況 ／ #24 体感スコア「+N P」＋ニコ動コメント ／
   #14 結果10カウントのカット割(#47 プレミア=暗転プチュン / #48 カウントは"発表前") ／ #25 大会後リザルト(改善ヒント) ／
   #27 入場紹介＋スミさんの対戦相手ブリーフィング ／ #26 ライバルのリアルタイム実況。
   ※お祈り/立ち絵カットは画像生成待ち→**仮(名前＋カット割)で骨組み**を作る方針。
2. **温度/スロットの残り**: #46 R2にも吸い出しと同じニードル温度ゲームを入れるか（**3択でオーナー要再判断**・前回dismiss） ／
   #49 パッキー点灯(ペカ)で**自動拡大**（reel.js の bonusWait タップ待ち撤去）。
3. **日常/経済/UX**: #41 体力を蓄積制(寝る=全回復/恋人=好感度で一定) ／ #44 フレーバー持参制(不足→スミ案内の強制購入/無一文→DA1回分) ／
   #23 行動後シーケンス(暗転→スロット→ステUP→次の日・DAY表示と被らせない) ／ #42 ch1ステ上がりすぎ→微減 ／ #39 バイト報酬3000は安い ／
   #33 スロットが招待行動で回らない ／ #34 スロット消化→LIME→次の日 の順 ／ #32 DAY2の強制職場行きに理由 ／ #35 みんとday5「あと3日」日数ズレ ／ #36 マップ初回チュート(点滅説明)。
4. **みんとナラ＋大物**: #2 みんと買い物イベント作り直し(tonariへフレーバー/方向音痴で駅まで案内・**初の素はここ**・説明ナレ削る・fifthで初扱い再主張しない) ／
   #3 みんと観戦=**rival↔supporter設計矛盾**(ch1で「みんとも出る」→ch3+応援側・台詞でなく設計判断) ／ #31 みんとの店で主人公はコンカフェと知らず戸惑う ／
   #9 tonari統合(場所で管理・テスト巡回改修要) ／ #16 ch2マップ(1章へも移動) ／ #17 ch2の日常会話(お疲れ/すごかったよ) ／
   #43残 なるの敗者復活背景(ch4/characters・廃止と矛盾) ／ #10 LIME7日目(day7=大会当日なので仕様通りの可能性)。

### ★設計タスク #53: ステータスがミニゲーム（特に温度コントロール）に効くように
**問題**: 序盤から温度コントロール等がうまくいき過ぎ＝ステ育成の意味が薄い。**まず設計案を出すのもタスク**。
- 現状の効き具合（既存）: HOLEカーソル速度=技術／HEATジャスト窓=根性／吸い出しは固定気味。これを**序盤=難/育成で楽**へ。
- たたき台案（要検討・要バランステスト）:
  - **吸い出し**: 針速度＝技術／センスで可変（低=速くシビア・高=ゆっくり）。適温の許容幅＝センスで可変（低=狭い）。
    1回で動かせる温度(PULL_DELTA)も技術で可変。低根性=温度がわずかに揺れる(ドリフト)＝保持が難。
  - **R2温度**: 低センスだと「現温度」表示がやや曖昧（±でブレて見える）→ 高センスでピタッと見える。
  - **HOLE/HEAT**: 既存の技術/根性依存を序盤はもっと厳しく（カーソル速め・ジャスト窓狭め）、育成で緩和。
  - 方針: 数値は見せず体感で。CLAUDE.md「育成で詰まない/そこそこ有利」を厳守（極端にしない）。playthroughは中ステで勝てる範囲に。
- 実装場所: stepPull(針速度/PULL_DELTA/適温許容)・stepAdjust(#45表示の揺れ)・stepFoil(speed)・stepCoalFire(justHi/baseRate)。

### 進め方の推奨 / テスト互換のキー
- contained修正はその場で→fixごとに build2本＋playthrough(最低)。大物は1パス集中。
- **テスト互換**: R2は「このままでいく」を残す／炭セットは「トライアングル」「炭4個」ラベル／適温は `__pullDebug` が動的target追従／HEATは `__heatDebug`／審査は `.trial-appeal[data-backed][data-cat]`＋`#trial-doubt[data-need]`／結果10カウントは pointer-events:none。
- 大会フロー: setup→theme→mix→pack→foil→**coal(炭セット)→coalfire(HEAT)**→steam→**adjust(R2温度)**→focus→pull→（r3_end）→**flavorTrial**→finishTournament→showResult→**runResultCountdown**→reveal。
- デプロイ: 古い表示なら Settings→Pages→Source を「GitHub Actions」に。mainへはPR（直push禁止）。数値非表示・★とランク。

## 次回への引き継ぎ（2026-06-13 第23便・UX大量修正＋HEAT/スロット）

**この便でやったこと（オーナーのプレイ報告に基づく一括修正）。開発は `claude/great-galileo-omzoob`。**

### コミット済み（テスト緑: playthrough/ch2/screenshots/reel/kuji/balance/map_hover）
- **A1 #2 HEAT IGNITION**: 炭起こしを「炭を目で見て芯がピカッと閃いた瞬間に取る」方式へ刷新。
  3つの炭が黒→赤→赤熱→ピカン→白熱→灰かぶりと変化。出力 `tt.coalFire` 維持＋`tt.coalResult` 追加。
  `stepCoalFire()`／CSS「HEAT IGNITION」節／SFX.spark／`__heatDebug` フック。
- **UX修正バッチ（commit b0fb597）**:
  - #5 会話の変な改行（固定文字数で名前が割れ末尾1字が孤立）→ 「ん」「小書きカナ」を行頭禁則に追加＋
    残り2字以下は折らず吸収。engine.js `autoWrap`。
  - #4 長文でウィンドウが伸びる → 4行/ページで**改ページ**（クリックで送る）。engine.js `typeText`/`renderPage`。
  - #6 行けない場所が薄すぎて消えて見える → `#map-pins .spot-pin:disabled` を opacity 0.85 でグレー明示。
  - #7 情報パネルがピンを隠す → `#map-pins` を z-index 4（#map-info より前面）＋「家」ピンを移動。
  - #8 誘いの日に同店2回 → `playLimeEvent` で会ったキャラの当日 `dayVisited` を記録。
  - #11 穴あけで狙い位置が見えない → アクティブリング上に脈動する `.hole-aim` 輪を表示。
  - #15 調整(R2)で再パック/再蒸らし可能 → 廃止。炭の位置・数のみ（温度は吸い出しで）。
- **#12 スロット配線（commit c67684d）**: オーナーGOで温存解除。docs/pakki_slot_spec.md §10準拠。
  **行動後に自動で1回転**（endActionでREEL.onAction）／**アプリ建て付けは出さない**（introDone=true で
  初回説明スキップ）＝パッキーは理由を説明されない謎のマスコット兼司会として説明なく回す。
  reel.js を index.html・standalone に同梱、reel.css 連結、reel用SFX追加、CONFIGにスロット演出。

### 第25便で完了・コミット済み（contained修正の束）
- **#38 適温=テーマ依存**（pullTargetZone）／ **#28 穴あけ切り上げ**（1/2/3周）／ **#37 スロット狙え撤去**＋**#40 バケ7-7-BAR**
- **#45 R2に温度可視化**（projectedTempを温度バー表示＋炭で適温に寄せる）／ **#50 炭2個削除・4個で大昇温・フラット/キューブ炭で温度差**
- **#51 炭焼きの熱が入る順をランダム**／ **#52 ジャスト色を赤さ残し**（真っ白回避）
- **保留/未**: **#46**（R2にも吸い出しと同じニードル温度ゲーム＝R2/R3両方ニードルにするか…オーナーが質問dismiss＝要再判断）／
  **#47**（プレミア=暗転プチュン）／**#48**（10カウントは発表前）／**#49**（パッキー点灯で自動拡大＝reel.jsの bonusWait タップ待ち撤去）

### 追加コミット済み
- **★A1（大会パート大改修）4要素 完成**: HOLE穴あけ / HEAT炭起こし / **FLAVOR TRIAL審査** /
  RESULT 10カウント が全て実装済み（master_spec 第2部・本作の華）。
- **A1 #3 FLAVOR TRIAL（審査）**: 提供後・結果発表の前に挿入。コンセプト(=theme)×アピールポイント
  (holeResult/coalResult/craftから自動生成)を審査員のザワザワに「ぶつける」。実績で裏打ち=納得↑／
  口だけ=「香ってない！」／見当違い=「ズレてる！」。presentationResultを総合スコアに上乗せ。
  `flavorTrial()`。ch1/ch2両方のpull→結果の間に挿入。CLAUDE.mdのプレゼン廃止を本仕様で上書き済。
- **A1 #4 RESULT 10 COUNT（結果発表10カウント）**: showResult前に集計中→10…0→プチュン(1位)/
  パリン(2位以下=GAME OVER)。`runResultCountdown()`。カウント中は大会画面に切替＆空にして取りこぼし回避。
- **#12 MOKUMOKUパッキー**: 名称をPUFF!PUFF!→MOKUMOKUに（もくもく＝煙）。行動後に自動回転・脱アプリ。
- **#19 改行エディタ**: `web/tools/linebreak_editor.html`。本編autoWrapと同規則でプレビュー＋手動改行。

### このセッション(第24-25便)で完了・コミット済み
- 炭の流れ: 「炭をコンロにセット(配置選択)→ HEAT IGNITION(取り出し)」に並べ替え（チュート/本番/バイト同順=#21の一部）。
  HEATは**7割焼けからスタート**（生焼け待ちなし）・炭数は配置選択(2/3/4)に従う。
- **#22** 章タイトル「一引き目」→「一吸目」。
- **#29/#30** 会話ウィンドウ: 文字**24px**に拡大・高さ**152px固定**・**最大2行で改ページ**（engine.js MAX_PAGE_LINES=2）。

### ★大会演出オーバーホール（第24-25便の最重要・互いに噛み合うので一気にやるのが綺麗）
- **#20 ブロック分割＋MC実況**: 工程をブロックに分け、各ブロック頭にMC実況風前振り（「はじめ選手、吸い出しに入った！」）
  ＋掛け合い。毎ゲーム後ではなく数回に分けて。淡々だと南雲票の逆転が響かない。既存3ラウンド(ch1_tournament_r1〜r3_end)を強化。
- **#27 入場演出**: プロレス風キャラ入場紹介＋**スミさんが対戦相手を画像つきで特徴解説**する事前ブリーフィング（順番に）。
- **#24 可視スコア＋ニコ動コメント**: **体感スコア(審査員票とは別系統)**を作り、JUST等で「+N P」をポップ＋
  ニコ動風に流れる顧客/審査員コメント（「もう完成したの!? +1p」）。提供スピードボーナス等も可視化して気持ちよく。
- **#14 結果10カウントのカット割**: runResultCountdownを強化。中央カウントの周りに **はじめ＋ライバルのお祈りカット＋作ったシーシャ**。
  お祈り画像は後で画像生成→**今は「名前＋カット割」の仮**で骨組みだけ作る。
  - **#47 プレミアのフライング**: 1位プレミアの早抜けプチュンを**スロットのブラックアウト→プチュン**風に（暗転→プチュン）。
  - **#48 カウントは"発表前"**: 10カウントは結果発表の**前のbuildup**として明確化。今は「結果発表へ」ボタン後に出て
    “発表時/発表後”に見えるので、FLAVOR TRIAL講評→（カウント=buildup）→発表 の順序・文言を整える。
- **#25 大会後リザルト**: 終了後に出来の内訳（穴あけA/炭B等）を出す＝次回への改善ヒント。
- **#26 リアルタイム感(将来)**: 大会中に他ライバルの動き実況通知（「なる、提供開始」）。#20の実況に同梱。
- **#28 穴あけ切り上げ**: HOLEを各リングで「切り上げる」＝**1週/2週/3週を選べる**（全3リング強制をやめる・少周は均等度で評価）。

### 日常/スロット/UXの不具合・調整
- **#23 行動後シーケンス**: 行動後に画面うっすら暗転→「ロード風の一拍」でスロット(MOKUMOKU)が回る→ステUP表示→次の日。
  今は stat-up と「DAY X」表示・LIMEと被る。順序を整理（#33/#34と一体）。
- **#33 スロットが招待行動で回らない**: 誘い/デート経由の行動が endAction(REEL.onAction) を通っていない疑い→必ず回す。
- **#34 スロット×LIME順序**: スロットを消化してからLIME(朝)→次の日。LIMEでスロットが見えない瞬間をなくす。
- **#32 強制イベントの唐突さ**: DAY2で2行動後に強制で職場行きが唐突→理由をつける（or 唐突さをなくす）。
- **#35 みんとday5 LIME**: 「あと3日だね」が日数と合わない（大会day7想定）→修正（該当台詞は要特定）。
- **#36 マップ初回チュートリアル**: 初めてマップに出た時、該当箇所が点滅して「ここはこういう機能」と説明。
- **#37 スロットの“狙え”撤去**: reel.js:599「赤7を狙えっ！/BARを狙えっ！」は実際押せない（自動回転）→
  狙え/押せ系を外し、勝手にいい感じの演出に（揃うか！？等の見せ方へ）。
- **#40 スロットのバケ役を 7-7-BAR に**: REG(バケ)の出目を BAR-BAR-BAR ではなく **7-7-BAR** に（reel.jsの役/ストリップ構成）。
- **#38 吸い出しの適温をテーマ依存に**: PULL_TARGET(=[0.63,0.79]固定) を tt.theme で可変に
  （リラックス=低め/高火力=高め…客の需要で適温が変わる）。stepPull。
- **#45 調整(R2)に温度可視化＋調整ゲーム性**: 炭替え・調整で**今の温度**を表示し、適温からズレてたら炭(位置/数)で寄せる。
  ＝stepAdjust に現温度ゲージ＋適温ターゲット、炭変更で温度が動く。#38(適温=テーマ依存)・#15(調整=炭のみ)と地続き。
  温度系（#38/#45/#15）はまとめて1パスで実装するのが綺麗。
- **#39 バイト報酬が安い**: 3000円は安すぎ→引き上げ（balance.mjsは最低8000円想定。3000が出る経路を調査）。
- **#41 体力は蓄積制に**: 1日経過で全回復をやめ、疲労を蓄積。回復は「家で寝る=フル回復／恋人=好感度に応じた一定回復」。
  あえて行動せず休む選択に意味を持たせる。advanceDay の addStamina(sleep) を見直し、休む/恋人ちょい会いに回復を寄せる。
- **#42 ch1のステータス上がりすぎ**: 現状ch1で上がりすぎ→**もう少しだけ下げる**（CLAUDE.md「ch4末で全100が頑張れば到達」基準）。
  gainStat 呼び出し/イベント報酬の係数を微減。playthrough は craft>=80 で南雲票勝ちなので過度に下げると勝てなくなる点に注意・要再テスト。
- **#44 フレーバー持参制（新システム）**: 大会までに自分のフレーバー在庫を用意。足りなければ**スミさん案内の強制購入イベント**、
  どうしても金が無ければ**スミさんがお情けでダブルアップル1回分**だけくれる。フレーバー在庫state＋大会突入時のゲート＋
  購入導線＋スミさんの救済を実装。ミックス画面は在庫から消費する形へ。
- **#43残: なるの敗者復活戦バックストーリー**: ルール側は廃止反映済(2位以下=GAME OVER)。但し ch4_main.json/characters.json の
  なるの「アジアで負け→敗者復活を勝ち抜いた」設定が残る＝廃止と矛盾。なるの背景を別ルート(連敗からの再挑戦等)に書き換えるか要判断。

### みんと/ナラティブ
- **#2 みんとのプライベート描写（買い物イベント作り直し）**:
  - **建て付けを自然に**: outing_minto_1 を「みんとが tonari にフレーバーを買いに行きたい→方向音痴で、はじめが駅まで案内」
    という自然な premise に作り直す（現状の“私服/制服参考の買い物”をやめる）。
  - **ここが初の素**: 買い物イベントでこそ はじめが**初めてみんとの素**を見る＝みんとの口調も素寄りに、はじめの反応もここに置く
    （今は口調がいつもの店モードで×）。
  - **説明ナレは削る**: 「このとき(素モード)は—」的な解説を入れず**プレイヤーに気づかせる**（お姉さん=みんとの発見を奪わない）。
  - **後続と矛盾させない**: ch1_minto_fifth 等の後の好感度イベントで「初めて素を見た」と再主張しない（1ビートに集約）。
  - 対象: outing_minto_1(ch1_events)／ch1_minto_fifth(ch1_minto)／date arrive-mid(game.js 1491-)。
- **#3 みんと観戦（設計判断が要る）**: オーナーは「みんとの14日目LIMEで観客席から見てる風＝矛盾」と指摘。但し
  **ch2のデータに該当LIMEは無い**（ch2 day14は直で決勝へ・ch2みんとはDAY12の店での警告のみ）。観客席/応援系の
  みんと台詞は**全てch3**（世界大会の応援）。＝ch1で「みんとも出る（もっと上へ）」と言うのにch3+で応援側に回る
  **rival↔supporter の設計矛盾**。台詞修正でなく「みんとを競技者として扱い続ける/応援に回る理由をつける」判断が要る。
- **#31 みんとの店**: 主人公は「コンカフェ」と知った風に言わない→最初は戸惑い、みんとにリード/ノリを作られる描写へ(ch1_minto_first)。

### 既存の大物（前セッション積み残し）
- **#9 tonari統合**: つむぎ会話・練習・バイト（・スミさん）を tonari 1スポットに集約（場所で管理）。本体＋テスト巡回の改修要。
- **#16 第2章マップ** / **#17 ch2日常会話**（お疲れ/すごかったよ）。
- **#10 LIME7日目が大会** → day7はch1大会当日なので仕様通りの可能性大（#35と関連・要確認）。
- **デプロイ**: 古い表示が残るなら Pages は旧ビルド。Settings→Pages→Source を「GitHub Actions」に。

## 次回への引き継ぎ（2026-06-13 第21〜22便・A1着手）

**現状サマリ**: main は最新（PR #77済・Pages公開中 https://haseatsu114514-dot.github.io/shisha-game/）。
日常システム大改修＋恋愛/くじ/レーダー/体力は反映済み。第1章タイトルは「一吸目（ファーストドロー）」。
**A1（大会パート大改修）に着手し、4要素のうち2つ目まで完成**。開発は引き続き `claude/great-galileo-omzoob`。

### A1 進捗（docs/master_spec.md 第2部が仕様）
- ✅ **#1 HOLE RHYTHM BATTLE（アルミ穴あけ）完成**: 円周カーソル＋均等度評価。外周→中周→内周、
  カーソルは何周でも回せ「次の周へ/穴あけを仕上げる」で進む。黒地×ネオンUI＋均等度メーター＋
  BEAUTIFUL RING等の演出。採点は従来互換(tt.foilHits)＋tt.holeResult(FLAVOR TRIAL用)も生成。
  `stepFoil()`（web/js/game.js）。CSSは style.css の「HOLE RHYTHM BATTLE」節。
- ✅ **アルミ盤テクスチャ取り込み口**: `assets/ui/foil_taut.png` を中央210%ズームで使用（しわ縁をクロップ）。
  **未アップなので今は金属グラデにフォールバック**。オーナーが foil_taut.png をアップ→build2本で反映。
  build_standalone は存在時のみ埋め込み。
- ✅ **2位以下＝GAME OVER**（master_spec改定）: 1位のみ進出。2位以下は賞金なしで GAME OVER 画面
  （「もう一度挑戦する」＋「タイトルに戻る」）。showDefeat()。勝利画面は gameover クラス除去済み。
- ✅ **#2 HEAT IGNITION（炭起こし）完成**: コンロの3つの炭を「目で見て」観察し、芯が一瞬ピカッと
  閃いた瞬間に取り上げる方式へ（ゲージ停止を廃止）。黒→赤→赤熱→ピカン→白熱→灰かぶりと色が変化、
  ジャストで最高評価。炭ごとに焼ける速度をずらし、根性でジャスト窓がわずかに広がる。出力は従来互換
  tt.coalFire("perfect"/"good"/"miss") ＋ tt.coalResult（justCount/readyCount/earlyCount/overheatCount・
  heatPower/heatStability/aromaRetention/burnRisk・coalFlashSuccess。FLAVOR TRIAL用）。実装は stepCoalFire()
  （web/js/game.js）、CSSは style.css「HEAT IGNITION」節、SFX.spark 追加。テスト用フック __heatDebug。
  steps.mjs/screenshots.mjs の炭操作を __heatDebug 駆動に更新。playthrough/ch2 緑で1位クリア確認。
- ⬜ **#3 FLAVOR TRIAL（審査）未**: コンセプト(=theme)×アピールポイント(holeResult/coalResult/craftから自動生成)を
  審査員のザワザワにぶつける。用語は「コンセプト/アピールポイント/ぶつける」、反論は「香ってない！」。
  弾丸系は使わない（#17）。CLAUDE.mdの「プレゼン廃止」はこのFLAVOR TRIALで上書き予定（着手時に更新）。
- ⬜ **#4 RESULT 10 COUNT（結果発表）未**: showResult の前に10カウント演出。1位=プチュン/2位以下=パリン。
  プチュン＝1位確定。審査員4分割カットイン。

### テスト（重要・次便で最適化）
- ✅ **6本すべて緑**: playthrough / ch2 / map_hover / kuji / balance / reel / screenshots。
  HEAT IGNITION は steps.mjs / screenshots.mjs とも `__heatDebug` 駆動で取り頃をつかむ。
- ✅ **screenshots 敗北ルートの赤を修復**（#2 とは別問題の既存テスト不具合）: 練習ドリル(HOLE)を
  開いたまま startTournament() を呼ぶと突入待ちループがドリルで即 break し本番大会に入れなかった。
  敗北セットアップ前にドリルを終わらせマップへ戻すよう screenshots.mjs を修正。ゲーム側の
  敗北→GAME OVER 経路自体は正常（rank=4 → onGameOver → showDefeat → GAME OVER を確認済み）。
- ⚠ ch2 / screenshots は新ミニゲームの自動操作で所要時間が長め（外部タイムアウトに注意。本体は健全）。
- foilテクスチャ未アップ中の404はplaythroughでフィルタ済（良性）。
- 起動: `python3 -m http.server 8123` 後、`node web/test/<name>.mjs`。ビルド: build_data→build_standalone。

### 注意
- リール（PUFF!PUFF!パッキー日常リール）は別スレッドで開発中のため**本編未配線のまま**（衝突回避）。
  reel.js等は同梱。配線手順は docs/pakki_slot_spec.md §10（向こうが固まってから一箇所で）。

## 次回セッションへの引き継ぎ（2026-06-13 第17〜20便終了時点）

**開始手順**: shisha-game 起点 → `claude/great-galileo-omzoob` をチェックアウト →
`python3 web/build_data.py && python3 web/build_standalone.py` →
テスト5本（playthrough / ch2 / screenshots / kuji / balance / map_hover）で健全性確認。
`python3 -m http.server 8123` を起動してから各 `node web/test/*.mjs`。

**この期間で実装したもの（docs/master_spec.md が唯一の仕様書・状態管理）**:
- 日常システム大改修: 名前開示(？？？)・好感度二層化(ポイント→段階)・体力(スタミナ)・
  同店1日1回＋定休日・夜→必ず帰宅就寝・会話自動改行・凛は1コマ消費/買物のみ無料・機材売却
- 経済: バイト最低8,000円・大会賞金(優勝5万/2位2万/3位1万)
- 恋愛: 告白を店外＆主人公主導(あげは除く)・恋人Lv5段階・コマ消費なしちょい会い・
  デート誘いLIME(1コマ消費＋好感度大)・恋愛イベント20本(つむぎ/みんと/凛/あげは×Lv1-5、lover_events.json)
- LIME: 1日1話題・連絡先 limeContacts ゲート・つむぎ交換ビート・誘いはシステムの声で説明
- 吸い出しに「ジャスト」判定／用語集に追記／旧大会ドキュメントの弾丸系に廃止注記
- シーシャくじ（A2・仮実装）: ボックス制・期待値設計(g500フル105%/ラストワン込み115%)・
  リセマラ防止シャッフル・抽選演出・凛のメタ発言。data/kuji.json。賞品アイテム/専用演出は今後
- ステータスメニューをタブ式に＋A4レーダーチャート(章開始時と重ね描き=成長可視化)
- キャラ別ステータス配分 CHAR_STAT(ch1-4)＋マップの事前ヒント＋カンスト時ランダム振替
- A3体力/A5経済バランスを balance.mjs で検証（通常安定・無理で寝込み・黒字維持）
- アダム外国人口調除去・DA立ち位置修正・年確シーン簡素化・素みんと口調・tonari背景最新版

**未対応（次やる）**:
- **A1 = 大会パート大改修（#16/第2部）**: HOLE RHYTHM BATTLE / HEAT IGNITION /
  FLAVOR TRIAL / RESULT 10カウント。**本作の華・最重要。Fableで実装する想定**（オーナー指示）。
  用語は #17（コンセプト/アピールポイント、弾丸系禁止）を最初から適用。CLAUDE.mdの
  「プレゼン廃止」はこの新FLAVOR TRIALで上書きされる旨を着手時に更新すること。
- ch3ブラウザ対応（ましろ恋愛イベント Lv1-5 を lover_events.json に同型追加・mukai・作戦会議3択）
- くじの賞品アイテム本体＆専用演出の本実装（現在は仮）
- 立ち絵 spriteScale の実値調整（足場のみ実装済。本番素材が来たら数値調整）
- 浮気/病み分岐・誠実ルート専用イベント（恋人システムの残り。#24）

**GitHub Pages**: 公開URL https://haseatsu114514-dot.github.io/shisha-game/ が 404
（Site not found）。ワークフロー(.github/workflows/deploy-pages.yml)は web/dist/** push で
起動するが、**Settings→Pages→Source を「GitHub Actions」にしないと公開されない**。
オーナーがこのトグルを設定すれば claude/** への push で自動公開される。
暫定プレイは web/dist/shisha_ch1.html をブラウザで直接開く（全アセット埋め込み済）。

## 2026-06-12 設計完了・実装待ちパッケージ: PUFF!PUFF!パッキー（日常リール）

ジャグラー型スロット（Aタイプ＋天井・1行動1回転・完全告知・お助けガチャ）の
**実装待ちパッケージ**。オーナー意向で**本編にはまだ接続しない**
（他機能の開発と並走中のため。GOが出たら任意のセッションで組み込める）。

- 正典・組み込み手順: **`docs/pakki_slot_spec.md`**（仕様全文＋貼り付け用スニペット）
- 本体: `web/js/reel.js`（抽選コア＋演出。単体で完結・既存ファイル無変更）
- スタイル: `web/css/reel.css`（組み込み時に style.css へ連結）
- テスト: `node web/test/reel.mjs`（決定論・最低+1保証・天井・フリーズ制御・裏確変・
  チェリー重複・出目整合を検証。本編未接続でも常に有効）
- 要点: 恩恵はステのみ（直前の行動で伸びたステのアンコール抽選、ハズレでも+1）／
  天井持ち越し（ゾーン8G・本天井40G）／フリーズは1セーブ1回の見えない制御／
  引き弱には裏確変／決定論シードでリセマラ不可／初回のみパッキーの説明

## 次回セッションへの引き継ぎ（2026-06-12 第16便終了時点）

**開始手順**: shisha-game 起点で新セッションを開く → mainに未マージの
最新開発ブランチ（今回: `claude/great-galileo-omzoob`）をチェックアウト →
`python3 web/build_data.py && python3 web/build_standalone.py` で再ビルド →
テスト3本（playthrough.mjs / screenshots.mjs / ch2.mjs）で健全性確認。

**立ち絵シートの受け取り**: オーナーが `assets/sheets_inbox/sheet_{id}.png`
（横5表情・単色背景）をGitHubウェブからアップしてくる運用
（手順と取り込みチェックリストは `assets/sheets_inbox/README.md`）。
届いていたら `tools/split_sheet.py` で取り込み → `make_face_icons.py` →
build 2本 → **engine.js の BG_FULL_PORTRAITS からそのキャラを外す** → テスト。
あげは（青背景・ヒョウ柄ギャル）と黒ショートボブ＋ライダースの女性
（おそらく凛の新デザイン。**idはオーナーに要確認**）のシートが生成済みとのこと。

**現状**: **ch1とch2がブラウザで通しプレイ可能**。準備期間は14日制。
- **ch2（HAZE: OPEN CLOUD）**: ch1クリア画面の「第2章へ進む」→ 章カットイン →
  ch2_opening。試合は DAY8予選 → DAY11準決勝 → DAY14決勝の3戦
  （`CHAPTERS`/`CH2_STAGES`、craft基準 58/70/80、1位のみ通過・敗北はリトライ可）。
  課題はストロベリー2g（`CH2_REGULATION`、flavors.json に strawberry 追加済み）。
  DAY5夜から**味覚スランプ**（ミックス画面の説明文が▒で霞む）。夜イベント:
  DAY2深淵バイト/DAY4炭落とし/DAY5スランプ/**DAY6店の匂い**/DAY7あげは励まし/
  勝利後にアダム・なる離反/**DAY10スミさんの沈黙**/DAY12みんと/DAY13つむぎ/
  決勝後「誰もいない優勝」→章クリア。朝LIME: DAY3博士の噂/DAY6組長の噂
- **恋愛システム**: 好感度5段階（AFFINITY_CAP=5）。ROMANCEABLE
  （tsumugi/minto/rin/ageha）が5到達→行動の区切りで告白イベント
  （confession.json をバンドル済み、accept/rejectは engine の next_id 第4引数で検知）。
  **2人目からは浮気警告**（cheat_warning、進むと guilt+1）。交際後は
  **絆レベル**（loveLevel 0〜5、gainAffinityが自動で切替）。ステータス画面に
  「恋人」バッジ＋絆ハート表示。state.lovers / loveLevel / guilt は浮気リスク
  システム（ch3修羅場）の土台
- UI: 立ち絵1人=中央/2人=左右・**大きめ表示**（TARGET 102/上限165、bgfull 92%）。
  章タイトルカットインは金グラデ文字＋ローマ数字透かし版。CONFIG/GALLERY/
  セーブスロット3/実況ティッカー/BGMループフェード実装済み。
  LIMEはchapterフィールドでフィルタ（未指定=ch1）。ch1のDAY8/10/12に
  つむぎ・なる・みんとのLIME追加済み

**立ち絵の取り込みフロー（5枚シート運用）**: ユーザーは立ち絵を
「横5表情並び・単色背景（緑/マゼンタ）」のシートで支給する。
`python3 tools/split_sheet.py sheet.png --char {id} --names normal,smile,surprise,sad,serious`
で分割＋クロマキー透過＋位置揃えして取り込む（表情の並び順は --names で合わせる。
等幅でないシートも透過後の隙間で自動分割）。取り込み後は build 2本と
`tools/make_face_icons.py` を再実行。

**未実装・次の課題（優先順）**:
1. **アセット**: 立ち絵は**5表情シート（横並び・単色背景）で支給予定** →
   `tools/split_sheet.py` で取り込み。凛・あげは・れい・組長の立ち絵／CG16枚／
   背景の本番化／コールドオープン用背景。立ち絵差し替え後は make_face_icons.py
2. **ch2の日常コンテンツ拡充**: 訪問スポットがch1の流用（REPEAT_VISIT）のまま。
   ch2専用の訪問会話・凛の味覚異変検知イベント・あげは訪問スポット
   （ageha の LIME 既存データを活かすには visits.ageha を立てる導線が必要）
3. **浮気リスクシステムの本格稼働（ch3）**: guilt は積み始めた。ニアミス分岐・
   閾値「呼び出し」・バッドエンド「ホワイトアウト」＋「二股の林檎」獲得は未実装
4. **ch3ブラウザ対応**: 東京編。mukai・ましろ・作戦会議3択。データ未バンドル
5. **大会パートのライブ感強化**（アイデア節の2〜8。1の実況ティッカーは実装済み）
6. **ch2のLIME**: ch2期間中は意図的に通常LIMEを止めている（離反演出）。
   噂LIME（rumor 4通: ch2は博士の人体実験噂DAY3・組長のシガーマン噂DAY6）は
   第14便で配信されるようになった。キャラの通常LIME（chapter:2 指定）の
   追加は引き続き任意
7. GitHub Pages有効化（オーナー作業: Settings→Pages→GitHub Actions）

※敗者復活戦のゲーム内実装は**ユーザー意向で不採用**（なるのルート演出としてのみ存続）

## 2026-06-12 セッションで実装済み（第13便: ch2＋恋愛システム）

- **第2章まるごと**（上記「現状」参照）。テスト `web/test/ch2.mjs` 新設
  （告白→恋人化→浮気警告→guilt、ch2の3戦通し、スランプUI、離反会話の出現を検証）
- **恋愛システム**（5段階→告白→浮気警告→絆5段階）
- **立ち絵さらに拡大**＋**章タイトルカットインのリッチ化**（金グラデ・透かし数字）
- steps.mjs がストロベリー課題に対応（mix-totalの表記で自動判別）

## 2026-06-12 セッションで実装済み（第12便: UI大幅改善＋14日制）

- **立ち絵レイアウト統一** — 1人なら画面中央（`pos-center`）、2人で左右
  （`pos-left`/`pos-right`、`#vn-portraits.duo` で幅制限）。
  enter/active のtransform競合も修正。bgfull素材は bottom5%・高さ84%に拡大
- **立ち絵5枚シート分割ツール** — `tools/split_sheet.py`。単色背景の5表情シートを
  クロマキー透過→透過後の隙間で自動分割→共通キャンバスの下端中央に配置
  （表情を切り替えても位置・サイズがズレない）
- **会話ツールボタン改善** — AUTO/SKIP/LOG/用語/MENU を枠の下（画面最下端・極小）
  から**枠上辺の右側**へ移動し大型化（「押しづらい」対応）
- **CONFIG実装** — テキスト速度4段階（engine `getTextSpeed`）・オート速度3段階・
  BGM/SFX音量スライダー（sfx.js `setBgmVolume`/`setSfxVolume`）。
  `shisha_config_v1` に永続化。タイトルとMENUの両方から開ける
- **GALLERY実装** — `D.cgs` のCG一覧をグリッド表示。閲覧済み（engine `onCg` →
  `shisha_gallery_v1`、セーブと独立）のみ開ける。クリックで全画面ビュー。
  CGアセットが入り次第そのまま機能する
- **セーブスロット** — オートセーブ＋手動3スロット（`shisha_ch1_slot1〜3`）。
  タイトルLOADはスロット選択式に変更、MENU（ステータス画面）にセーブ/ロード/
  コンフィグボタンを追加。セーブに `savedAt` を記録
- **タイトルBGMループ調整** — 切り出し音源の継ぎ目を timeupdate でフェードアウト/イン
- **パッキー実況ティッカー** — 穴あけ/炭起こし/引きの判定に即反応する実況テロップ
  （`#tn-ticker`、大会本番のみ。ライブ感アイデア#1）
- **章タイトルカットイン** — `showChapterTitle()`（黒地＋金罫＋明朝、
  pointer-events:none でクリック非遮断）。NEW GAMEのコールドオープン直後に
  「第一章 一吸目 -ファーストパフ- SMOKE CROWN CUP編」を表示。ch2以降も使い回せる
- **14日制** — `MAX_DAYS=14`。夜イベント再配置（中間チェックDAY3→7、リハーサル
  DAY6→13、前夜DAY7→14）、スミさんの日替わり一言を14日分に拡充、
  チュートリアル後の台詞・DAYカードの残日数を `MAX_DAYS` 連動に

## 旧引き継ぎ（2026-06-11終了時点）

**当時の未実装リスト**: アセット／浮気リスク／ch2対応／敗者復活戦（→不採用）／
ライブ感強化／ch3-4分岐／UI残（GALLERY・CONFIG・セーブスロット・BGMループ→実装済み）／
GitHub Pages有効化

## 2026-06-11 セッションで実装済み（第5便: 7日間のペース改善＋大会3ラウンド制）

- **練習＝本番の部分練習** — 練習メニューを本番ミニゲームのドリルに刷新
  （穴あけ/炭起こし/引き/集中＋従来の提供イメトレ）。`startDrill`/`finishDrill`。
  **自己ベスト（practiceBest 0〜2）が大会本番のスコアに上乗せ**（最大+8）
- **バイトのオーダーチャレンジを大会同様のフル工程に**（mix→pack→foil→coalfire→
  coal→steam→pull）。客のリクエストは日替わり（`dailyTheme`、DAYカードと
  マップ情報に表示）
- **DAY3夜: 中間チェック** — スミさんが素の一台を講評（技術22で分岐）
- **DAY6夜: 前日リハーサル** — 短縮通し（theme→…→pull）。出来（_rehearsal_tier）が
  本番ボーナス＋スミさんの講評
- **教わった技ボーナス** — LIMEイベント完了フラグ（_ev_*）で本番に小ボーナス:
  なる夜テストラン=蒸らし5/8分で+3、なる買い出し=+2、みんと昼設計=relax/fruity
  テーマで+3、アダム哲学/みんと接客=プレゼン+2
- **大会3ラウンド制** — STEP_FLOW に adjust（R2: 一箇所だけ調整やり直し）を追加。
  steam後に ch1_tournament_match→r1_end、focus後に r2_end、present後に r3_end を
  接続（未使用だったラウンド会話を全部使用）。工程遷移は `tnNext`（フロー定義準拠）
  に共通化
- **DAYカード** — スミさんの日替わり一言（day3/6の夜イベント予告を兼ねる）＋
  今日の客層を表示
- みんとの正体バレの認識描写を「tonariに来るときの姿と同じ」に明確化
- テスト: ドリル/調整ラウンド対応（steps.mjs のボタン検索を #tn-body にスコープ）

## 2026-06-11 セッションで実装済み（第4便: 恋愛強化＋CG基盤）

- **CG（スチル）基盤** — `docs/cg_production_plan.md` に14枚の仕様＋生成プロンプト。
  show_cg の組み込みは全箇所済みで、`assets/cgs/cg_*.png` を置いて再ビルドすれば
  自動表示（無い間は何も出ない・404も出ない。`D.cgs` 一覧で存在チェック）。
  build_standalone の埋め込み対象も `cg_*.png` 全部に拡大
- **みんと正体バレの一本化** — 正史バレは `ch1_minto_fifth`（5回目訪問の私服デート。
  既存シーンに身分証→自称20歳の嘘ビートを追加、表情を ura_* に）。
  `outing_minto_1` は「初めて会った気がしない」ニアミスに変更（CG cg_minto_casual 付き）
- **つむぎの反転ビート** — `ch1_tsumugi_smoke_color` を拡張: 共感覚を打ち明け、
  「大会の日の煙……濁らせちゃ、だめですよ」と初めて"命令"する
  （優勝の夜の「濁ってた」への伏線として接続）
- **デート翌朝のフォローLIME** — outing完了で `_outing_done_*` フラグ →
  翌朝チャットが届く（minto=素が漏れるきゅん版／naru／adam）。
  trigger_condition "flag" を limeDueMessages に追加。
  好感度上限時のLIME返信報酬は魅力+2にフォールバック
- 恋愛設計（反転ビート4人・香り識別子・1デート1きゅん）を
  brand/story_and_structure.md に明文化

## 2026-06-11 セッションで実装済み（第3便: ch1の引き強化＋矛盾修正）

- **コールドオープン**（`ch1_cold_open`）— NEW GAME直後、暗転＋煙でch4ドバイ決勝の
  一瞬 →「──1年前。」→ tonariへ。本番背景ができたら差し替え
- **南雲の「懐かしい味だ」＋白衣の男** — ch1_tournament_after のロビーに追加
- **つむぎの「濁ってた」** — 優勝の夜のtonariに追加（慢心の伏線）
- **優勝の夜のスマホ**（`postClearPhone` / `phoneShowCustom`）— なるの採点表どんでん
  （優勝は南雲の1票だけ）→「？？？」の試合データ記録通知 → クリア画面。
  playthrough.mjs が出現を検証
- **みんと裏イベント** — チュートリアル直後に私服の素みんと（speaker `oneesan`、
  立ち絵 minto の `ura_*`）が客で来店、デビュー一台＋年齢確認ギャグ
  （`ch1_tutorial_oneesan`）。回収は outing_minto_1（正体バレ→身分証の記憶→
  自称20歳の嘘に接続。みんとの立ち絵もこのシーンは ura_* に変更）
- **LOGウィンドウ実装** — vn-log ボタンで直近200行のバックログ表示（残件消化）
- **ストーリー矛盾の修正**（brand/story_and_structure.md）:
  ch2旧稿の「亜鉛で味覚回復」結末を廃止し「東京へ（味覚は戻らないまま）」に差し替え／
  【転】勝ち上がりブロックを離反・優勝の前へ移動（時系列整理）／
  炭落とし事故の重複を解消（深淵のみに）／ch3「助けられた」を新正史に修正

## 2026-06-11 セッションで実装済み（後半）

- **スマホ用うっすら操作パッド** — タッチ端末（pointer: coarse）の会話画面にだけ、
  右端に半透明（opacity 0.26）の AUTO / SKIP / ▼ パッドを表示（#touch-pad）。
  触れている間と AUTO/SKIP 作動中だけはっきり浮かぶ（.lit）。PCでは出ない
- **ドクター・ケムリ → チャコール博士に改名** — 全md/json/gdを置換
  （「ケムリクサ」=なるの店は対象外として保護）。dialogue の speaker
  `kemuri` → `dr_kemuri` に正規化（idは据え置き）
- **ストーリー大改定（全採用）** — `brand/story_and_structure.md`:
  - スミさんが折れた理由を「勝つための煙で客の顔が見えなくなった」に（ch2のはじめの鏡像、
    ch4特訓「俺はお前に同じ顔をさせたくなかった」→帰国告白で回収）
  - ch4 の優勝の一台を**ダブルアップル**に（ch3事件の贖罪＋アダムへのリスペクト）
  - ch5 全面改稿: タイトル「-EN:CODE-」回収（煙=生き方のエンコード、吸う=デコード）、
    9000も「合わせる」ことはできる→決め手は「賭けて差し出すか」、チャコール博士=味覚を
    失った元職人、採点UIなし（挙手制）、9000は破壊せず教材として残る、
    哲学候補リスト（令和の茶室・時間を引き延ばす技術・煙は残らない・レシピはコピー
    できる/エンコードはできない・受け手も半分）
  - 全編モチーフ「店の匂い」追加（ch1なるの店問答が初出、ch4ラストで自分の匂いに気づく）
  - characters.json（sumi/dr_kemuri/shisha_9000）と CLAUDE.md
    （ストーリー整合ルール節を新設）も同期済み

## 2026-06-11 セッションで実装済み

- **LIME（朝のスマホ演出）** — `data/lime_messages.json` をエンジン接続。
  日付が変わった朝にスマホUI（#phone-overlay、LINE風チャット）でメッセージが届く。
  - 種類: 招待（受けると**行動を消費せず**イベント発生。昼=閉じた足で、夜=最初の
    行動後に発生）／返信付きチャット（返信で好感度+1）／大会朝の応援（根性+2）
  - LIME交換済み条件: `LIME_EXCHANGE_VISITS`（なる=2回・アダム=3回は会話内に
    交換シーンがあるためその訪問回数、他は1回）
  - 招待イベント `interaction_naru_night_01` / `interaction_minto_noon_01` を
    `ch1_events.json` に新規執筆（outing_naru/adam/minto も web にバンドル済み）
  - state: `limeDone`（既読ID）/ `pendingLimeNight`（夜の約束）。旧セーブ互換あり
- **顔ドット絵アイコン** — `tools/make_face_icons.py` が立ち絵の頭部を切り出して
  48pxドット絵化（`assets/ui/face_icons/face_{id}.png`、data URIでバンドル）。
  好感度バナーのバッジ（旧: 名前の頭文字「つ」「な」）・マップピンのキャラスポット・
  LIMEのアバターに使用。頭文字はアイコンが無いキャラのフォールバックとして残存
  - naru/adam/ryuji は背景込みplaceholder一枚絵のため TUNE の abs 指定で顔位置を
    手調整している。**立ち絵を差し替えたら `python3 tools/make_face_icons.py` を再実行**
- **なるのLIME文面・outing_naru_1 を新人格（人格者の好敵手）に書き直し**
  （旧イキリ口調が data/lime_messages.json に残っていた）
- テスト: playthrough.mjs がLIME出現を検証（出ないと失敗）。スマホとマップ操作の
  競合はリトライで吸収

## 引き継ぎメモ（2026-06-10 時点の最新状態）

- 開発ブランチ: `claude/hopeful-ride-5rg3zg`（mainに未マージ。続きはここから）
- **タイトルロゴ**: ユーザー製ロゴ画像（黒地・墨筆風「水煙前線」）はチャット内
  画像のためファイル未着。暫定で `tools/make_title_logo.py` による生成ロゴを
  `assets/ui/ui_title_logo.png` に配置済み。**ユーザーからロゴPNGがファイル添付で
  届いたら同パスに上書き → `python3 web/build_data.py && python3 web/build_standalone.py`
  で反映**（黒背景つきならクロマキー等で透過化してから）
- **タイトルBGM**: `assets/audio/bgm/title.mp3`（ユーザー提供 Hookah Midnight Loop、
  64kbps）。スタンドアロンには先頭1.2MB（約150秒）を埋め込み
- **タイトルキャラ**: `TITLE_CHARA_POOL`（game.js）= tsumugi / sumi / packii /
  naru / adam / minto からランダム。アート一枚絵を煙マスクの窓
  （#title-chara-window、CSSの多層radial-gradientマスクを揺らす）に
  Ken Burnsズーム付きで表示。bbox情報は build_data.py の portrait_trim
  （l/w を追加済み）から取得
- **会話の背景込み立ち絵**（.portrait.bgfull）も同じ煙マスク表示に変更済み
- **ロゴ**: `tools/make_title_logo.py` を参考画像準拠に作り直し
  （水パイプのシルエット・渦煙・青/紫インク飛沫・-EN:CODE-長ダッシュ）
- **一人称視点**: はじめの立ち絵は出さない（engine.js `NO_PORTRAIT_SPEAKERS`）
- **チュートリアル**: opening後に1回だけ（`state.flags._tutorial_done`）。
  フローは game.js `TUTORIAL_FLOW` / ヒントは `TUTORIAL_TIPS`

## 2026-06-10 セッションで実装済み（後半）

- **タイトル画面 v2** — 墨ベースの背景＋漂う煙レイヤー、右側にカラフルな煙星雲と
  ランダムキャラ（つむぎ/スミ/パッキー、`TITLE_CHARA_POOL`）。ロゴ・メニューの
  時間差フェードイン、メニューホバーの金バー演出
- **タイトルBGM** — `assets/audio/bgm/title.mp3`（Hookah Midnight Loop)。
  自動再生ブロック時は最初のタップ/キーで再試行
- **チュートリアル** — オープニング後にスミさんの作業台で1回シーシャ作りを通し体験
  （`TUTORIAL_FLOW`: テーマ→ミックス→パック→穴あけ→炭起こし→配置→蒸らし→引き、
  各ステップにスミさんのアドバイス表示、結果に応じた講評＋技術/センス上昇）。
  `state.flags._tutorial_done` で1回のみ
- **一人称視点** — 主人公はじめの立ち絵は表示しない（`NO_PORTRAIT_SPEAKERS`）

## 2026-06-10 セッションで実装済み

1. **タイトル刷新** — 新ロゴ「水煙前線 -EN:CODE-」、左寄せ縦メニュー
   （NEW GAME / LOAD / GALLERY / CONFIG / EXIT）。GALLERY・CONFIG・EXIT は
   ダミーで disabled
2. **ダイアログUI刷新** — 紫の花飾りネームプレート、金縁＋四隅装飾のテキスト枠、
   右下に AUTO / SKIP / LOG / MENU ツール、左上に「⚓ 場所」HUD、
   右上に「🔥 Lv.X」HUD（5ステータス平均から算出）
3. **マップをタップ式に** — `#map-pins` にシールド型ピンを座標配置。
   ロックは鍵アイコン＋グレースケール。右下に金バナーの情報パネル、
   左上に Sakae 風の DAY カード（日数・行動・所持金）
4. **立ち絵の拡大** — 切り抜き素材は TARGET 88% / 最大 145% でバストアップ気味に。
   背景込み素材（naru/adam/minto/ageha/mashiro/ryuji）には `.portrait.bgfull`
   クラスを当て、上下グラデマスクで枠に収める
5. **演出** — ボタンに :active scale、選択肢に hover scale + shadow glow

## 2026-06-10 追加実装（夜）

- **キービジュアル1号**を `assets/ui/title_arts/title_art_keyvisual_01.png` に
  配置（ユーザーがGitHub Webからアップ→リネーム）。タイトル右側に表示中
- **バイト基本給 2,500→6,000円**（data/baito_events.json）
- **バイトのオーダーチャレンジ**: バイト後の選択肢で「一台作る」を選ぶと
  短縮フロー（ミックス→パック→配置→蒸らし→引き）でお客さんの一台を作る。
  テーマはお客さんのリクエスト（ランダム）。出来でチップ 500〜5,000円
  ＋技術UP。`beginMaking("baito")` / `BAITO_FLOW` / `finishBaitoOrder`
- **機材ショップ**: マップに「店」ピン追加（行動を消費しない）。
  equipment.json の price でch1機材を購入。初期所持は
  `STARTER_EQUIPMENT`（シリコンボウル/ロートスハガル/フラット炭）のみ。
  大会の機材選択は所持品だけが並ぶ（買い物が攻略に直結）
- **マップの墨金テーマ化**: DAYカード・マップ切替チップをピンク→墨×金に。
  ピンのブレ修正（拡縮を .shield 内側に移動）
- **キャラ名を白に統一**（SPEAKER_COLORS 廃止）
- **煙遷移の「間」**: engulf を 1.4→2.6秒に。白で包む→ひと呼吸→ゆっくり晴れる
- **タイトルロゴを上に移動**（top 9%→5%）

## 2026-06-10 ストーリー大改定（深夜）

- **主人公を再定義**: 序盤は名店tonariの看板で調子に乗るカーズ/TOD2カイル型。
  夢は「自分の店を持つ」（中身のない夢）。ch2で味覚喪失＋仲間の離反
  （テイルズオブジアビス型）、ch3でましろだけに救われる。
  `brand/story_and_structure.md` 全面更新済み
- **なるを人格者の好敵手に変更**: 惜しみなく教える・負けを認める・
  はじめの慢心に正面から苦言を言う唯一の人物。ch1_naru.json 全5話書き直し
- **ch1_opening / ch1_tournament_after 書き直し**: 新性格反映。
  優勝インタビューに3択（慢心/素直/ちゃっかり、set_flagで記録→ch2で参照予定）
- **新規シナリオデータ（ch2/ch3用・エンジン未接続）**:
  - `data/dialogue/ch2_isolation.json` — なるの直言・アダム/みんと/つむぎの離反・
    誰もいない優勝
  - `data/dialogue/ch3_double_apple.json` — mukai到着・**ダブルアップル事件**
    （シトラスミントをダブルアップルと言われて同調→嘘がバレる）・毎日の一杯・味覚回復
- characters.json の hajime/naru 人格記述を更新、CLAUDE.md に新設定を明記

## 次回セッションの修正タスク

1. **GitHub Pages 開通の確認** — ユーザーが Settings で有効化したら
   空コミットpushでデプロイし、URLの200を確認する
3. **背景込み立ち絵の本番素材** — bgfull リスト（naru/adam/minto/ageha/
   mashiro/ryuji）を切り抜き素材に差し替えれば大きく出せる。
   `tools/pixelize.py` で透過・トリム・パレット統一が可能
4. **マップピン座標の手調整** — `SPOT_LAYOUT` は仮配置。
   背景画像の建物に合わせて x/y を微調整したい
5. **LOG ウィンドウ** — `vn-log` ボタンは未実装（toast のみ）
6. **CONFIG / GALLERY** — タイトルメニューのダミーをアクティブ化
7. **タイトルBGMのループ調整** — スタンドアロン版は曲の途中でループが
   先頭に戻る（1.2MB切り出しのため）。気になるならフェード処理を入れる

## 画像生成パイプライン（統一感を出す方法）

スタイルの統一はプロンプトで頑張らず、**後処理スクリプトで揃える**:

1. 生成時は透過を要求しない。**単色背景（マゼンタ #FF00FF など）で生成**
2. `tools/pixelize.py`（次回作成）で一括処理:
   クロマキー透過 → 余白トリム → 共通グリッドに縮小 → 共通パレットに減色
   → ドットの粒度が全キャラ・全背景で自動的に揃う
3. キャラの同一性は「マスター1枚を作って、それを参照画像に表情差分を
   依頼する」方式（gpt-image-1 / NanoBanana の image-to-image）
4. ファイル名規約: `chr_{id}_{face}.png` / `bg_{place}_{time}.png` を厳守すれば
   ビルドスクリプトが自動で拾う



1. **大会の3ラウンド制** — `ch1_tournament_r1_end〜r3_end` の会話が未使用。
   ラウンド間で機材・配合を調整できると本家仕様に近づく
2. **敗者復活戦** — 2位以下の救済ルート（本家仕様: 枠1〜2）
3. **好感度イベントの深掘り** — confession.json（outing系は2026-06-11にLIME経由で接続済み）
4. **第2章** — ch2_main.json 等のデータは既にある。エンジンはそのまま使える
5. **タイトル/大会ステージの本番アセット差し替え**

## ビルド・テストコマンド

```bash
python3 web/build_data.py          # data/ → web/js/data.js
python3 web/build_standalone.py    # → web/dist/shisha_ch1.html（要 pillow）
npx http-server -p 8123 &          # リポジトリルートで
node web/test/playthrough.mjs      # 通しプレイ（要 playwright）
node web/test/screenshots.mjs      # 敗北ルート＋スクショ
```
