# ブラウザ版 開発方針・ロードマップ

このファイルは「Claudeをメイン開発者にしてゲームを完成させる」ための
作業方針。新しいセッションを始めたら、まずこれを読むこと。

## 開発の原則

1. **データが唯一の正** — シナリオ・キャラ・フレーバー等は `data/` のJSONに置く。
   ブラウザ版 (`web/`) と Godot 版は同じJSONを読む。シナリオ追加はJSONに書く。
2. **ブラウザ版をメインの開発トラックにする** — Claudeはこの環境で
   ブラウザを起動して自動プレイ・スクリーンショット確認まで自走できる。
   Godot はこの環境で実行できないため、Godot 版は「演出強化版」として後追い。
3. **毎回テストを通す** — `web/test/playthrough.mjs`（優勝ルート）と
   `web/test/screenshots.mjs`（敗北→再挑戦）を変更のたびに実行する。
4. **演出（ジュース）を仕様として明示する** — 「動く」と「気持ちいい」は別タスク。
   新機能には必ず「SE・アニメーション・画面遷移演出」の項目を含めて依頼する。

## ユーザー（オーナー）との分業

| 担当 | 内容 |
|---|---|
| ユーザー | プレイして気になった点を箇条書きで送る／画像生成（キャラ・背景・CG）／方針決定 |
| Claude | 実装・演出・データ組み込み・自動テスト・バランス調整・アセット仕様書/プロンプト作成 |

### フィードバックの送り方（効くやつ）

- 「○○がしょぼい」だけでなく「どうなってほしいか」を1行添える
  （例:「ミックス画面、注ぐたびにボウルに色が溜まってほしい」）
- スクリーンショット添付があると最速
- 1メッセージに何件入れてもよい。Claudeが分解して全部対応する

## アセット差し替えパイプライン

1. Claude が `アセット差し替え進捗管理表.csv` を基に生成プロンプト一覧を作る
2. ユーザーが画像生成AIで作成し、所定のパス（`assets/backgrounds/` 等）に
   同名で上書き
3. Claude が `python3 web/build_data.py && python3 web/build_standalone.py` で
   再ビルド（立ち絵は透過余白を自動計測して表示を正規化する）

現状プレースホルダのままの主な背景: `bg_title` `bg_tournament_stage`
`bg_shop` `bg_home` `bg_naru_shop` `bg_adam_shop` `bg_street_day/night`
`bg_tonari_outside`

## 実装済み（第1章）

- 会話エンジン（dialogue JSON互換・タイプライター・立ち絵2スロット＋自動サイズ正規化）
- 日常ループ（7日×昼夜2行動・バイト・練習・キャラ訪問・スポット解禁制・日替わりカード）
- SMOKE CROWN CUP（機材選択→テーマ→12g配合→パッキング→アルミ穴あけ→炭起こし→
  配置→蒸らし→集中→引き→プレゼン→採点、作業台リグのリアルタイム可視化）
- 演出: WebAudio合成SE・BGM・判定スタンプ・煙/泡パーティクル・結果リビール
- 16:9固定ステージ（スマホ横向き対応）・localStorageセーブ・1ファイル配布ビルド

## 公開URL（プレイ・確認用）

- **開発中ブランチの最新（push 直後に反映・動作確認済み）**:
  `https://raw.githack.com/haseatsu114514-dot/shisha-game/<ブランチ名>/web/dist/shisha_ch1.html`
  - 初回アクセス時に raw.githack の確認ページが出る →「Open the page」を1タップ
- **安定版（未開通）**: https://haseatsu114514-dot.github.io/shisha-game/
  - `.github/workflows/deploy-pages.yml` が `web/dist/` を GitHub Pages へ配備
    （main と claude/** ブランチの push がトリガー）
  - ⚠️ **ユーザーの作業待ち**: Settings → Pages → Source を「GitHub Actions」に
    する必要がある（workflowのトークンでは初回有効化が403で失敗する。
    実行ログで確認済み）。有効化後に再push（空コミットでよい）すれば公開される
- jsDelivr / statically.io は HTML を text/plain で返すため使えない（検証済み）
- スマホは横向き推奨（縦だと回転ヒントが出る）。PC/スマホどちらも同じURLでOK

## 2026-06-11 セッションで実装済み（第6便: 新ヒロイン凛＋デート口実）

- **新ヒロイン: 匂坂 凛（27歳・年上枠・ch1から解放）** — 問屋街の卸直営店
  『Dr.fookah』（旧称「機材ショップ」を改名。1階=物販＋試飲席、2階=ショールーム）。
  海外メーカーNIGHTSIDE日本代理店。はじめを「試香モルモット」として雇う。
  ショップ画面の「2階」ボタンから1日1回・行動消費なしで訪問
  （`ch1_rin_first/second/third/repeat`、RIN_SEQUENCE/doRinVisit）。
  3回目で**限定フレーバー「NS アールグレイ（試作）」解放**（spiceカテゴリ初＝
  余韻/高火力テーマの2マッチが初めて可能になる。requires_flag _rin_sample）
- 凛のLIME（モニター報告 lime_rin_report）、confession_rin、ending（修羅場選択肢・
  sit_with_rin・shuraba_rin・epilogue_rin、CGポイント cg_rin_confession/epilogue）
- **つむぎ取材デート** — lime招待（好感度3）→ outing_tsumugi_1（絵の取材・
  「お店の人を困らせたくない」の一線台詞入り）→ 翌朝フォローLIME
- **デート口実の設計**と**浮気リスクシステムの設計**（ch2-3実装予定）を
  brand/story_and_structure.md に明文化。CLAUDE.md ヒロイン5人化
- ⚠️ **凛の立ち絵が未生成**（chr_rin_*.png）。visual_concepts は characters.json に
  記載済み。立ち絵が来たら make_face_icons.py 再実行で顔アイコンも自動生成

## 2026-06-11 セッションで実装済み（第7便: レギュレーション＋凛デザイン）

- **ch1レギュレーション「ミント2g以上」実装** — 大会・前日リハーサルのミックスで
  課題未達は提出不可（`CH1_REGULATION`/`activeRegulation`、パッキーの告知付き）
- **中間順位発表** — R1/R2終了会話の後に暫定順位パネル（`showStandings`）。
  南雲票が入るまでは演出上**最大2位止まり**（最終採点には影響しない）。
  前園の渋い講評→「総合印象点は最終発表で入る」の引きで逆転の気持ちよさを作る
- **章別レギュレーション表を正史化**（story doc/CLAUDE.md）: ch2=ストロベリー
  （スランプに残酷な高難度）、ch3=自由＋ましろ作戦会議3択、ch4=自由＋日本アピール
  ＋スミさん作戦会議3択、ch5=グレープ単体の技術勝負
- **凛のデザイン確定**: 鈍色ブルーのロング低め結び・無色ネイル・銀フープ・
  深夜だけ黒縁眼鏡。**推しはブルーベリー**（repeatイベントに反映）。
  visual_concepts更新済み（立ち絵生成はこのプロンプトで）
- **ch3控室の「モテモテ」いじり分岐**を設計（修羅場/のろけ/なし、story doc）
- **バッドエンド「ホワイトアウト」仕様**（特殊ED扱い・ロード再開で半減・
  レシピ「二股の林檎」獲得。secret_recipes.jsonに実装済み）

## 2026-06-11 セッションで実装済み（第8便: 勝敗条件＋審査員持ち点投入制）

- **ch1の勝敗条件を変更** — craft（一台の出来）が基準（80）未達なら南雲の持ち点が
  動かず**普通に敗北**。基準超えで最終発表時に南雲が持ち点10を一括投入し
  **必ず逆転優勝**（`NAGUMO_CRAFT_BAR`、finishTournament）。リザルト講評にも反映
- **審査員持ち点投入制**をルールの軸として正史化（story doc/CLAUDE.md）:
  各審査員=持ち点10・好きなタイミングで投入・戻せない。前園は早入れ派、
  南雲は最後まで温存する流儀。**敗者復活戦は余り点が原資**（なるのルートの背骨）。
  中間発表パネルに「実況パネル: 前園R1で3点投入／南雲まだ動かず」を表示

## 2026-06-11 セッションで実装済み（第9便: 用語集＋リーフ規範＋ch5グレープ）

- **用語集（GLOSSARY）実装** — 会話画面の「用語」ボタンで `data/glossary.json` を
  表示（シーシャの仕組み／リーフ3種／道具／作り方／大会用語の5グループ）。
  シーシャ未経験者向けの解説を一級市民として扱う
- **リーフ規範** — 主人公はブロンドのみ（flavors.json に `leaf` フィールド、
  stepMix がフィルタ）。ダーク/シガーは用語・他キャラ演出専用。
  ましろは「ブロンドも作れるがシガー/ダーク好み」を正史化
- **ch5の課題をグレープ単体に変更**（DAはch4の優勝の一台で使用済みのため差別化）

## 2026-06-11 セッションで実装済み（第10便: バイト専用課題）

- **バイトの課題はお客さんのリクエスト**（大会レギュレーションとは別系統）。
  日替わり3種: テーマ希望のみ／指名フレーバー「ブルーベリーしっかり」（2g以上）／
  **「ミントは苦手で……」（ミント0g縛り）**＝接客文化ルール「ミント確認」のゲーム化
  （`baitoRequest`、activeRegulation がモード別に出し分け）。
  リクエストに応えた日は心づけ+500円。ミックス画面の表示・ボタン文言も対応

## 2026-06-11 セッションで実装済み（第11便: 最終調整）

- **ch4=和風一本（DA要素なし）／ch5=ダブルアップル単体に戻す**。
  ch3事件の贖罪とアダムへのリスペクトの回収は**ch5のDA単体勝負**へ移動
  （ch4決勝の一台は抹茶・柚子・ほうじ茶系の「和の煙」に書き換え済み）
- **ミント確認の廃止** — 毎回聞くのは不自然。苦手な客が自分から「抜きで」と言う
  形に（バイトの抜き注文リクエストはそのまま）。お姉さんイベントも客側から
  好みを言う形に書き換え。CLAUDE.mdの文化ルール更新
- **チップ文化の廃止** — 日本のシーシャ屋にチップはない。バイトの臨時収入は
  「店からの売上ボーナス／給料上乗せ／まかない」表現に全面変更。
  お姉さんのチップ描写・金銭報酬も削除（CLAUDE.mdに接客・金銭の規範を新設）

## 大会パートのライブ感 強化アイデア（次回実装候補・優先順）

「動く」は揃ったので、次は「会場にいる感じ」。実装コスト低い順:

1. **パッキー実況のリアルタイム割り込み** — ミニゲームの判定に即反応する実況
   テロップ（perfect→「うおーっと！ 完璧な穴あけ！」／miss→「あらら〜♪」）。
   画面上部に流れる実況バー（ticker）。既存の toast/showStamp を流用すれば数行
2. **観客の声ポップ** — 工程の節目に観客の一言が泡のように湧く
   （「いい匂いしてきた…」「4番テーブルの煙やばくない？」）。
   spawnBubbles の文字版。歓声SEは sfx.js に合成追加
3. **ライバル進捗チップ** — 作業中、画面端に「なる: 蒸らし中」「アダム: 提供完了」
   の小さなステータス表示。自分が遅いと焦る（実害なしの演出プレッシャー）。
   R2調整中に「なる、ここで炭を組み替えたッ！」と実況を挟む
4. **審査員リアクション＋持ち点投入演出** — 提供後に審査員ごとの一言カットイン、
   「前園、3点投入！！」をスタンプ演出で。持ち点制（実装済みのルール）が
   画面で動いて見えるようになる
5. **工程間のマイクロ掛け合い** — ミニゲームの合間に1〜2行の短い掛け合い＋
   マイクロ選択肢（なる「お前、手ぇ震えてんぞ」→「うるさい」/「ありがと」で
   根性/好感度に微反映）。テンポを殺さないよう各ラウンド1回まで
6. **観客ボルテージゲージ** — perfect連続でパッキーのテンション段階UP＋
   会場ゲージ上昇（見た目主体、満タンでプレゼンに微ボーナス）
7. **ラウンドカットイン＆BGM段階展開** — 「ROUND 2」の番組風カットイン
   （day-card流用）。R1→R3でBGMレイヤー追加、結果発表前は無音→ドラムロール
8. **劣勢演出** — 中間発表3位以下で画面がうっすら暗転＋ざわめきSE。
   逆転時のカタルシスの下ごしらえ

## 次回セッションへの引き継ぎ（2026-06-12終了時点・最新）

**開始手順**: shisha-game 起点で新セッションを開く → ブランチ
`claude/hopeful-ride-5rg3zg` をチェックアウト →
`python3 web/build_data.py && python3 web/build_standalone.py` で再ビルド →
テスト2本（playthrough.mjs / screenshots.mjs）で健全性確認。

**現状**: ch1はブラウザで完全プレイ可能。**準備期間は14日制**（中間チェック=DAY7夜、
リハーサル=DAY13夜、前夜=DAY14夜）。章開始時に章タイトルカットイン
（`showChapterTitle`）。立ち絵は**1人=画面中央／2人=左右**（engine.js
`layoutPortraits`）。CONFIG（テキスト速度/オート速度/BGM/SFX音量）・
GALLERY（CG閲覧記録 `shisha_gallery_v1`）・**セーブスロット3つ**（タイトルLOAD
とMENUから）・パッキー実況ティッカー（`pakkiLive`）・タイトルBGMのループ
フェード実装済み。テストは2本とも通る状態。

**立ち絵の取り込みフロー（5枚シート運用）**: ユーザーは立ち絵を
「横5表情並び・単色背景（緑/マゼンタ）」のシートで支給する。
`python3 tools/split_sheet.py sheet.png --char {id} --names normal,smile,surprise,sad,serious`
で分割＋クロマキー透過＋位置揃えして取り込む（表情の並び順は --names で合わせる。
等幅でないシートも透過後の隙間で自動分割）。取り込み後は build 2本と
`tools/make_face_icons.py` を再実行。

**未実装・次の課題（優先順）**:
1. **アセット**: 凛の立ち絵（青髪・visual_concepts記載済み）→ face_icons再生成／
   CG16枚（docs/cg_production_plan.md）／なる・アダム・りゅうじの立ち絵が
   同一placeholder／背景の本番化（bg_tournament_stage等）／コールドオープン用背景
2. **浮気リスクシステム**（設計済み: story doc「浮気リスクシステム」節。
   うしろめたさ内部値・ニアミス分岐・匂い/色で検知・バッドエンド「ホワイトアウト」
   ＋レシピ「二股の林檎」は secret_recipes.json に追加済み）
3. **ch2ブラウザ対応**: ストロベリー課題（味見ノイズ演出）・LIME離反演出・
   凛の味覚異変検知・あげは登場。データは ch2_main/ch2_isolation 等に既存
4. **大会パートのライブ感強化**（アイデア節の2〜8。1の実況ティッカーは実装済み）
5. **14日制の中だるみ対策**: DAY8〜12の夜イベント・LIMEが薄い。
   lime_messages.json の trigger_day は1〜6のままなので後半にも追加・分散したい
6. ch3控室「モテモテ」いじり分岐・ch3ましろ作戦会議3択・ch4スミさん3択（設計済み）
7. GitHub Pages有効化（オーナー作業: Settings→Pages→GitHub Actions）

※敗者復活戦のゲーム内実装は**ユーザー意向で不採用**（なるのルート演出としてのみ存続）

## 2026-06-12 セッションで実装済み（第12便: UI大幅改善＋14日制）

- **立ち絵レイアウト統一** — 1人なら画面中央（`pos-center`）、2人で左右
  （`pos-left`/`pos-right`、`#vn-portraits.duo` で幅制限）。
  enter/active のtransform競合も修正。bgfull素材は bottom5%・高さ84%に拡大
- **立ち絵5枚シート分割ツール** — `tools/split_sheet.py`。単色背景の5表情シートを
  クロマキー透過→透過後の隙間で自動分割→共通キャンバスの下端中央に配置
  （表情を切り替えても位置・サイズがズレない）
- **会話ツールボタン改善** — AUTO/SKIP/LOG/用語/MENU を枠の下（画面最下端・極小）
  から**枠上辺の右側**へ移動し大型化（「押しづらい」対応）
- **CONFIG実装** — テキスト速度4段階（engine `getTextSpeed`）・オート速度3段階・
  BGM/SFX音量スライダー（sfx.js `setBgmVolume`/`setSfxVolume`）。
  `shisha_config_v1` に永続化。タイトルとMENUの両方から開ける
- **GALLERY実装** — `D.cgs` のCG一覧をグリッド表示。閲覧済み（engine `onCg` →
  `shisha_gallery_v1`、セーブと独立）のみ開ける。クリックで全画面ビュー。
  CGアセットが入り次第そのまま機能する
- **セーブスロット** — オートセーブ＋手動3スロット（`shisha_ch1_slot1〜3`）。
  タイトルLOADはスロット選択式に変更、MENU（ステータス画面）にセーブ/ロード/
  コンフィグボタンを追加。セーブに `savedAt` を記録
- **タイトルBGMループ調整** — 切り出し音源の継ぎ目を timeupdate でフェードアウト/イン
- **パッキー実況ティッカー** — 穴あけ/炭起こし/引きの判定に即反応する実況テロップ
  （`#tn-ticker`、大会本番のみ。ライブ感アイデア#1）
- **章タイトルカットイン** — `showChapterTitle()`（黒地＋金罫＋明朝、
  pointer-events:none でクリック非遮断）。NEW GAMEのコールドオープン直後に
  「第一章 一吸目 -ファーストパフ- SMOKE CROWN CUP編」を表示。ch2以降も使い回せる
- **14日制** — `MAX_DAYS=14`。夜イベント再配置（中間チェックDAY3→7、リハーサル
  DAY6→13、前夜DAY7→14）、スミさんの日替わり一言を14日分に拡充、
  チュートリアル後の台詞・DAYカードの残日数を `MAX_DAYS` 連動に

## 旧引き継ぎ（2026-06-11終了時点）

**当時の未実装リスト**: アセット／浮気リスク／ch2対応／敗者復活戦（→不採用）／
ライブ感強化／ch3-4分岐／UI残（GALLERY・CONFIG・セーブスロット・BGMループ→実装済み）／
GitHub Pages有効化

## 2026-06-11 セッションで実装済み（第5便: 7日間のペース改善＋大会3ラウンド制）

- **練習＝本番の部分練習** — 練習メニューを本番ミニゲームのドリルに刷新
  （穴あけ/炭起こし/引き/集中＋従来の提供イメトレ）。`startDrill`/`finishDrill`。
  **自己ベスト（practiceBest 0〜2）が大会本番のスコアに上乗せ**（最大+8）
- **バイトのオーダーチャレンジを大会同様のフル工程に**（mix→pack→foil→coalfire→
  coal→steam→pull）。客のリクエストは日替わり（`dailyTheme`、DAYカードと
  マップ情報に表示）
- **DAY3夜: 中間チェック** — スミさんが素の一台を講評（技術22で分岐）
- **DAY6夜: 前日リハーサル** — 短縮通し（theme→…→pull）。出来（_rehearsal_tier）が
  本番ボーナス＋スミさんの講評
- **教わった技ボーナス** — LIMEイベント完了フラグ（_ev_*）で本番に小ボーナス:
  なる夜テストラン=蒸らし5/8分で+3、なる買い出し=+2、みんと昼設計=relax/fruity
  テーマで+3、アダム哲学/みんと接客=プレゼン+2
- **大会3ラウンド制** — STEP_FLOW に adjust（R2: 一箇所だけ調整やり直し）を追加。
  steam後に ch1_tournament_match→r1_end、focus後に r2_end、present後に r3_end を
  接続（未使用だったラウンド会話を全部使用）。工程遷移は `tnNext`（フロー定義準拠）
  に共通化
- **DAYカード** — スミさんの日替わり一言（day3/6の夜イベント予告を兼ねる）＋
  今日の客層を表示
- みんとの正体バレの認識描写を「tonariに来るときの姿と同じ」に明確化
- テスト: ドリル/調整ラウンド対応（steps.mjs のボタン検索を #tn-body にスコープ）

## 2026-06-11 セッションで実装済み（第4便: 恋愛強化＋CG基盤）

- **CG（スチル）基盤** — `docs/cg_production_plan.md` に14枚の仕様＋生成プロンプト。
  show_cg の組み込みは全箇所済みで、`assets/cgs/cg_*.png` を置いて再ビルドすれば
  自動表示（無い間は何も出ない・404も出ない。`D.cgs` 一覧で存在チェック）。
  build_standalone の埋め込み対象も `cg_*.png` 全部に拡大
- **みんと正体バレの一本化** — 正史バレは `ch1_minto_fifth`（5回目訪問の私服デート。
  既存シーンに身分証→自称20歳の嘘ビートを追加、表情を ura_* に）。
  `outing_minto_1` は「初めて会った気がしない」ニアミスに変更（CG cg_minto_casual 付き）
- **つむぎの反転ビート** — `ch1_tsumugi_smoke_color` を拡張: 共感覚を打ち明け、
  「大会の日の煙……濁らせちゃ、だめですよ」と初めて"命令"する
  （優勝の夜の「濁ってた」への伏線として接続）
- **デート翌朝のフォローLIME** — outing完了で `_outing_done_*` フラグ →
  翌朝チャットが届く（minto=素が漏れるきゅん版／naru／adam）。
  trigger_condition "flag" を limeDueMessages に追加。
  好感度上限時のLIME返信報酬は魅力+2にフォールバック
- 恋愛設計（反転ビート4人・香り識別子・1デート1きゅん）を
  brand/story_and_structure.md に明文化

## 2026-06-11 セッションで実装済み（第3便: ch1の引き強化＋矛盾修正）

- **コールドオープン**（`ch1_cold_open`）— NEW GAME直後、暗転＋煙でch4ドバイ決勝の
  一瞬 →「──1年前。」→ tonariへ。本番背景ができたら差し替え
- **南雲の「懐かしい味だ」＋白衣の男** — ch1_tournament_after のロビーに追加
- **つむぎの「濁ってた」** — 優勝の夜のtonariに追加（慢心の伏線）
- **優勝の夜のスマホ**（`postClearPhone` / `phoneShowCustom`）— なるの採点表どんでん
  （優勝は南雲の1票だけ）→「？？？」の試合データ記録通知 → クリア画面。
  playthrough.mjs が出現を検証
- **みんと裏イベント** — チュートリアル直後に私服の素みんと（speaker `oneesan`、
  立ち絵 minto の `ura_*`）が客で来店、デビュー一台＋年齢確認ギャグ
  （`ch1_tutorial_oneesan`）。回収は outing_minto_1（正体バレ→身分証の記憶→
  自称20歳の嘘に接続。みんとの立ち絵もこのシーンは ura_* に変更）
- **LOGウィンドウ実装** — vn-log ボタンで直近200行のバックログ表示（残件消化）
- **ストーリー矛盾の修正**（brand/story_and_structure.md）:
  ch2旧稿の「亜鉛で味覚回復」結末を廃止し「東京へ（味覚は戻らないまま）」に差し替え／
  【転】勝ち上がりブロックを離反・優勝の前へ移動（時系列整理）／
  炭落とし事故の重複を解消（深淵のみに）／ch3「助けられた」を新正史に修正

## 2026-06-11 セッションで実装済み（後半）

- **スマホ用うっすら操作パッド** — タッチ端末（pointer: coarse）の会話画面にだけ、
  右端に半透明（opacity 0.26）の AUTO / SKIP / ▼ パッドを表示（#touch-pad）。
  触れている間と AUTO/SKIP 作動中だけはっきり浮かぶ（.lit）。PCでは出ない
- **ドクター・ケムリ → チャコール博士に改名** — 全md/json/gdを置換
  （「ケムリクサ」=なるの店は対象外として保護）。dialogue の speaker
  `kemuri` → `dr_kemuri` に正規化（idは据え置き）
- **ストーリー大改定（全採用）** — `brand/story_and_structure.md`:
  - スミさんが折れた理由を「勝つための煙で客の顔が見えなくなった」に（ch2のはじめの鏡像、
    ch4特訓「俺はお前に同じ顔をさせたくなかった」→帰国告白で回収）
  - ch4 の優勝の一台を**ダブルアップル**に（ch3事件の贖罪＋アダムへのリスペクト）
  - ch5 全面改稿: タイトル「-EN:CODE-」回収（煙=生き方のエンコード、吸う=デコード）、
    9000も「合わせる」ことはできる→決め手は「賭けて差し出すか」、チャコール博士=味覚を
    失った元職人、採点UIなし（挙手制）、9000は破壊せず教材として残る、
    哲学候補リスト（令和の茶室・時間を引き延ばす技術・煙は残らない・レシピはコピー
    できる/エンコードはできない・受け手も半分）
  - 全編モチーフ「店の匂い」追加（ch1なるの店問答が初出、ch4ラストで自分の匂いに気づく）
  - characters.json（sumi/dr_kemuri/shisha_9000）と CLAUDE.md
    （ストーリー整合ルール節を新設）も同期済み

## 2026-06-11 セッションで実装済み

- **LIME（朝のスマホ演出）** — `data/lime_messages.json` をエンジン接続。
  日付が変わった朝にスマホUI（#phone-overlay、LINE風チャット）でメッセージが届く。
  - 種類: 招待（受けると**行動を消費せず**イベント発生。昼=閉じた足で、夜=最初の
    行動後に発生）／返信付きチャット（返信で好感度+1）／大会朝の応援（根性+2）
  - LIME交換済み条件: `LIME_EXCHANGE_VISITS`（なる=2回・アダム=3回は会話内に
    交換シーンがあるためその訪問回数、他は1回）
  - 招待イベント `interaction_naru_night_01` / `interaction_minto_noon_01` を
    `ch1_events.json` に新規執筆（outing_naru/adam/minto も web にバンドル済み）
  - state: `limeDone`（既読ID）/ `pendingLimeNight`（夜の約束）。旧セーブ互換あり
- **顔ドット絵アイコン** — `tools/make_face_icons.py` が立ち絵の頭部を切り出して
  48pxドット絵化（`assets/ui/face_icons/face_{id}.png`、data URIでバンドル）。
  好感度バナーのバッジ（旧: 名前の頭文字「つ」「な」）・マップピンのキャラスポット・
  LIMEのアバターに使用。頭文字はアイコンが無いキャラのフォールバックとして残存
  - naru/adam/ryuji は背景込みplaceholder一枚絵のため TUNE の abs 指定で顔位置を
    手調整している。**立ち絵を差し替えたら `python3 tools/make_face_icons.py` を再実行**
- **なるのLIME文面・outing_naru_1 を新人格（人格者の好敵手）に書き直し**
  （旧イキリ口調が data/lime_messages.json に残っていた）
- テスト: playthrough.mjs がLIME出現を検証（出ないと失敗）。スマホとマップ操作の
  競合はリトライで吸収

## 引き継ぎメモ（2026-06-10 時点の最新状態）

- 開発ブランチ: `claude/hopeful-ride-5rg3zg`（mainに未マージ。続きはここから）
- **タイトルロゴ**: ユーザー製ロゴ画像（黒地・墨筆風「水煙前線」）はチャット内
  画像のためファイル未着。暫定で `tools/make_title_logo.py` による生成ロゴを
  `assets/ui/ui_title_logo.png` に配置済み。**ユーザーからロゴPNGがファイル添付で
  届いたら同パスに上書き → `python3 web/build_data.py && python3 web/build_standalone.py`
  で反映**（黒背景つきならクロマキー等で透過化してから）
- **タイトルBGM**: `assets/audio/bgm/title.mp3`（ユーザー提供 Hookah Midnight Loop、
  64kbps）。スタンドアロンには先頭1.2MB（約150秒）を埋め込み
- **タイトルキャラ**: `TITLE_CHARA_POOL`（game.js）= tsumugi / sumi / packii /
  naru / adam / minto からランダム。アート一枚絵を煙マスクの窓
  （#title-chara-window、CSSの多層radial-gradientマスクを揺らす）に
  Ken Burnsズーム付きで表示。bbox情報は build_data.py の portrait_trim
  （l/w を追加済み）から取得
- **会話の背景込み立ち絵**（.portrait.bgfull）も同じ煙マスク表示に変更済み
- **ロゴ**: `tools/make_title_logo.py` を参考画像準拠に作り直し
  （水パイプのシルエット・渦煙・青/紫インク飛沫・-EN:CODE-長ダッシュ）
- **一人称視点**: はじめの立ち絵は出さない（engine.js `NO_PORTRAIT_SPEAKERS`）
- **チュートリアル**: opening後に1回だけ（`state.flags._tutorial_done`）。
  フローは game.js `TUTORIAL_FLOW` / ヒントは `TUTORIAL_TIPS`

## 2026-06-10 セッションで実装済み（後半）

- **タイトル画面 v2** — 墨ベースの背景＋漂う煙レイヤー、右側にカラフルな煙星雲と
  ランダムキャラ（つむぎ/スミ/パッキー、`TITLE_CHARA_POOL`）。ロゴ・メニューの
  時間差フェードイン、メニューホバーの金バー演出
- **タイトルBGM** — `assets/audio/bgm/title.mp3`（Hookah Midnight Loop)。
  自動再生ブロック時は最初のタップ/キーで再試行
- **チュートリアル** — オープニング後にスミさんの作業台で1回シーシャ作りを通し体験
  （`TUTORIAL_FLOW`: テーマ→ミックス→パック→穴あけ→炭起こし→配置→蒸らし→引き、
  各ステップにスミさんのアドバイス表示、結果に応じた講評＋技術/センス上昇）。
  `state.flags._tutorial_done` で1回のみ
- **一人称視点** — 主人公はじめの立ち絵は表示しない（`NO_PORTRAIT_SPEAKERS`）

## 2026-06-10 セッションで実装済み

1. **タイトル刷新** — 新ロゴ「水煙前線 -EN:CODE-」、左寄せ縦メニュー
   （NEW GAME / LOAD / GALLERY / CONFIG / EXIT）。GALLERY・CONFIG・EXIT は
   ダミーで disabled
2. **ダイアログUI刷新** — 紫の花飾りネームプレート、金縁＋四隅装飾のテキスト枠、
   右下に AUTO / SKIP / LOG / MENU ツール、左上に「⚓ 場所」HUD、
   右上に「🔥 Lv.X」HUD（5ステータス平均から算出）
3. **マップをタップ式に** — `#map-pins` にシールド型ピンを座標配置。
   ロックは鍵アイコン＋グレースケール。右下に金バナーの情報パネル、
   左上に Sakae 風の DAY カード（日数・行動・所持金）
4. **立ち絵の拡大** — 切り抜き素材は TARGET 88% / 最大 145% でバストアップ気味に。
   背景込み素材（naru/adam/minto/ageha/mashiro/ryuji）には `.portrait.bgfull`
   クラスを当て、上下グラデマスクで枠に収める
5. **演出** — ボタンに :active scale、選択肢に hover scale + shadow glow

## 2026-06-10 追加実装（夜）

- **キービジュアル1号**を `assets/ui/title_arts/title_art_keyvisual_01.png` に
  配置（ユーザーがGitHub Webからアップ→リネーム）。タイトル右側に表示中
- **バイト基本給 2,500→6,000円**（data/baito_events.json）
- **バイトのオーダーチャレンジ**: バイト後の選択肢で「一台作る」を選ぶと
  短縮フロー（ミックス→パック→配置→蒸らし→引き）でお客さんの一台を作る。
  テーマはお客さんのリクエスト（ランダム）。出来でチップ 500〜5,000円
  ＋技術UP。`beginMaking("baito")` / `BAITO_FLOW` / `finishBaitoOrder`
- **機材ショップ**: マップに「店」ピン追加（行動を消費しない）。
  equipment.json の price でch1機材を購入。初期所持は
  `STARTER_EQUIPMENT`（シリコンボウル/ロートスハガル/フラット炭）のみ。
  大会の機材選択は所持品だけが並ぶ（買い物が攻略に直結）
- **マップの墨金テーマ化**: DAYカード・マップ切替チップをピンク→墨×金に。
  ピンのブレ修正（拡縮を .shield 内側に移動）
- **キャラ名を白に統一**（SPEAKER_COLORS 廃止）
- **煙遷移の「間」**: engulf を 1.4→2.6秒に。白で包む→ひと呼吸→ゆっくり晴れる
- **タイトルロゴを上に移動**（top 9%→5%）

## 2026-06-10 ストーリー大改定（深夜）

- **主人公を再定義**: 序盤は名店tonariの看板で調子に乗るカーズ/TOD2カイル型。
  夢は「自分の店を持つ」（中身のない夢）。ch2で味覚喪失＋仲間の離反
  （テイルズオブジアビス型）、ch3でましろだけに救われる。
  `brand/story_and_structure.md` 全面更新済み
- **なるを人格者の好敵手に変更**: 惜しみなく教える・負けを認める・
  はじめの慢心に正面から苦言を言う唯一の人物。ch1_naru.json 全5話書き直し
- **ch1_opening / ch1_tournament_after 書き直し**: 新性格反映。
  優勝インタビューに3択（慢心/素直/ちゃっかり、set_flagで記録→ch2で参照予定）
- **新規シナリオデータ（ch2/ch3用・エンジン未接続）**:
  - `data/dialogue/ch2_isolation.json` — なるの直言・アダム/みんと/つむぎの離反・
    誰もいない優勝
  - `data/dialogue/ch3_double_apple.json` — mukai到着・**ダブルアップル事件**
    （シトラスミントをダブルアップルと言われて同調→嘘がバレる）・毎日の一杯・味覚回復
- characters.json の hajime/naru 人格記述を更新、CLAUDE.md に新設定を明記

## 次回セッションの修正タスク

1. **GitHub Pages 開通の確認** — ユーザーが Settings で有効化したら
   空コミットpushでデプロイし、URLの200を確認する
3. **背景込み立ち絵の本番素材** — bgfull リスト（naru/adam/minto/ageha/
   mashiro/ryuji）を切り抜き素材に差し替えれば大きく出せる。
   `tools/pixelize.py` で透過・トリム・パレット統一が可能
4. **マップピン座標の手調整** — `SPOT_LAYOUT` は仮配置。
   背景画像の建物に合わせて x/y を微調整したい
5. **LOG ウィンドウ** — `vn-log` ボタンは未実装（toast のみ）
6. **CONFIG / GALLERY** — タイトルメニューのダミーをアクティブ化
7. **タイトルBGMのループ調整** — スタンドアロン版は曲の途中でループが
   先頭に戻る（1.2MB切り出しのため）。気になるならフェード処理を入れる

## 画像生成パイプライン（統一感を出す方法）

スタイルの統一はプロンプトで頑張らず、**後処理スクリプトで揃える**:

1. 生成時は透過を要求しない。**単色背景（マゼンタ #FF00FF など）で生成**
2. `tools/pixelize.py`（次回作成）で一括処理:
   クロマキー透過 → 余白トリム → 共通グリッドに縮小 → 共通パレットに減色
   → ドットの粒度が全キャラ・全背景で自動的に揃う
3. キャラの同一性は「マスター1枚を作って、それを参照画像に表情差分を
   依頼する」方式（gpt-image-1 / NanoBanana の image-to-image）
4. ファイル名規約: `chr_{id}_{face}.png` / `bg_{place}_{time}.png` を厳守すれば
   ビルドスクリプトが自動で拾う



1. **大会の3ラウンド制** — `ch1_tournament_r1_end〜r3_end` の会話が未使用。
   ラウンド間で機材・配合を調整できると本家仕様に近づく
2. **敗者復活戦** — 2位以下の救済ルート（本家仕様: 枠1〜2）
3. **好感度イベントの深掘り** — confession.json（outing系は2026-06-11にLIME経由で接続済み）
4. **第2章** — ch2_main.json 等のデータは既にある。エンジンはそのまま使える
5. **タイトル/大会ステージの本番アセット差し替え**

## ビルド・テストコマンド

```bash
python3 web/build_data.py          # data/ → web/js/data.js
python3 web/build_standalone.py    # → web/dist/shisha_ch1.html（要 pillow）
npx http-server -p 8123 &          # リポジトリルートで
node web/test/playthrough.mjs      # 通しプレイ（要 playwright）
node web/test/screenshots.mjs      # 敗北ルート＋スクショ
```
